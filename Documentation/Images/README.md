# README screenshots

These images show Yap's native interface using sample data. They contain no private meeting content.

- `meeting-gallery.png`: README hero showing the actual native meeting interface. The isolated SDK-free fixture uses the production `MeetingView`, gallery, participant labels, and meeting controls with four fictional webcam portraits supplied through the existing native-video renderer interface. The portraits are AI-generated camera content; the app UI is not generated or composited. The fixture has no live account, connected call, camera, microphone, or screen capture.
  - Captured with the user-authorized native macOS window screenshot on September 7, 2026 (PDT), September 8, 2026 (UTC). The fixture renders the production `MeetingView` from release commit `283e37cfddc47cac213be884f5dab6582a8713b0`. The original **2624 × 1784-pixel RGBA PNG** includes the complete window, native macOS shadow, and transparent surroundings.
  - The original capture bytes are preserved without resizing, recompression, cropping, masking, or compositing. All four image corners are fully transparent; the alpha channel also contains partially transparent shadow pixels. This README image is not claimed to meet Zoom App Gallery's separate upload dimensions.
  - Use the complete gallery window with all four faces and the actual meeting controls visible. This demonstrates the interface, not live SDK acceptance or meeting capacity.
- `agenda.png`: the production `YapRootView` and `TodayView` in an isolated Interface Preview fixture with sample calendar events and the fictional display name Alex. The fixture uses its own bundle identity and preferences, with no live account or network access.
- `recordings.png`: the production recording views with an isolated local sample video, recording metadata, chat, and transcript fixture. The slide is an original sample presentation, not a customer meeting. The visible transcript search and selected passage use the real recording interface.
  - Both secondary views were captured with the native macOS window screenshot on September 7, 2026 (PDT), September 8, 2026 (UTC). Each original **2536 × 1696-pixel RGBA PNG** includes the complete window, native shadow, and transparent surroundings. The fixture links the production UI from release commit `283e37cfddc47cac213be884f5dab6582a8713b0`; its build metadata was recorded at `01078d5918db5bd8c33e368ec49fce030e4a03be`.
  - These capture bytes are also unchanged: no resizing, recompression, cropping, masking, or compositing. All four corners are fully transparent, and each alpha channel retains the partially transparent native shadow.

The Yap app icon is the original app artwork in `Resources/YapIcon.png`, with the Icon Composer source in `Resources/Yap.icon`. All three screenshots are native captures of the Yap interface. They replace the earlier JPEG previews and demonstrate the interface with sample content; they do not establish live SDK or account acceptance.
