# Yap security evidence for the Zoom review

Updated September 7, 2026 (Pacific), for **Yap 0.1.2, build 3**, source `01078d5918db5bd8c33e368ec49fce030e4a03be`. [PR #7](https://github.com/grinich/yap/pull/7) fixes the native updater startup failure found in 0.1.1. Its validation passed **644 Swift tests in 77 suites, 40 Python tests and 24 authorization-service tests**. The new actual-SDK release workflow, app/DMG notarization and independent downloaded-artifact checks passed; the exact-tag CodeQL scan also passed. Initial native and personal-mode recording/SDK checks passed; managed native authorization and broader acceptance remain pending. The immutable **0.1.1 and 0.1.0** tags/assets are superseded and retained only as historical evidence; do not install or promote those drafts. The [architecture diagram](../output/pdf/Zoom-Architecture.pdf) is available as a one-page PDF, with reproducible [source](Architecture/generate.py). This is an evidence inventory for Michael Grinich's application, not certification or an independent security assessment. CI, scan and notarization results are separate from production acceptance and portal submission.

**Do not promote 0.1.2.** Its signed artifacts and SAST passed, but the reproducible Keychain error when switching from saved personal developer configuration to managed Yap sign-in remains a release blocker. The final full-resolution native PNG screenshots are captured and integrated in the README locally; publishing them on GitHub remains required before shipping. [Capture provenance](Images/README.md). Provider branding is now saved and verified after reload: Google consent is **Yap**, its desktop client/project display names are **Yap for Mac**, and the current Zoom development/production app is **Yap**. Google remains **Testing**, with no public verification approval claimed. [Provider branding checkpoint](Distribution.md#provider-branding-checkpoint). Complete the corrected candidate and managed native acceptance before promotion; no replacement version is asserted here.

## Current candidate checkpoint

**Current source:** `01078d5918db5bd8c33e368ec49fce030e4a03be`, tag `v0.1.2`. [PR #7 CI 34180726835](https://github.com/grinich/yap/actions/runs/34180726835) passed before merge. The fix adds `SUVerifyUpdateBeforeExtraction=true` alongside the required signed feed. A regression test using the actual Sparkle updater rejects the previous configuration with error 5 and accepts the corrected one without performing an update check. These checks do not establish successful installation or update delivery.

The actual-SDK [release run 34181128184](https://github.com/grinich/yap/actions/runs/34181128184) passed and created unpublished draft **384418390** with five assets. Independent downloaded-artifact verification passed at **03:05:38 UTC on September 8, 2026**: all five GitHub digests, ZIP/DMG checksum files, app/DMG Developer ID signatures, stapled notarization, Gatekeeper and DMG integrity. CryptoKit independently verified both the ZIP signature and complete signed appcast content with the bundled public key; declared content length and trailing content were checked, and in-memory tampering was rejected. The downloaded bundle is **0.1.2, build 3**, `com.grinich.yap`, signed by team `VSVHNQP588`, with the workers.dev endpoint/feed and both `SURequireSignedFeed` and `SUVerifyUpdateBeforeExtraction` enabled. This verification step did not launch or install the app or demonstrate N → N+1 update delivery. [Current artifact evidence](Releases.md#current-candidate-evidence).

[Exact-tag CodeQL run 34181127659](https://github.com/grinich/yap/actions/runs/34181127659) passed at **03:16:39 UTC on September 8, 2026**, on `refs/tags/v0.1.2`, source `01078d5918db5bd8c33e368ec49fce030e4a03be`. Both analyses used **CodeQL 2.26.4** with `security-extended`; Swift **1738606742: 28 rules / 0 results**, JavaScript/TypeScript **1738537368: 103 rules / 0 results**. Both have empty analysis error and warning fields. Release-tag and repository-wide queries returned **zero open alerts** at **03:16:43 UTC**. These are uploaded rule counts, not counts of security-only queries; zero findings do not establish absence of vulnerabilities. The two-page `Yap-SAST-Evidence-v0.1.2.pdf` is rendered, visually inspected and staged locally for portal upload. It is **9,057 bytes**, SHA-256 `ffea8f3ab5b34723e9c783bdfc17bac6869d0fa3a8f8ef3d0ffb5e7aad6412f3`; no public PDF URL or completed portal upload is asserted.

Native **0.1.2 (build 3)** installation checks passed About/version/icon, the login-item toggle (original state restored), and updater startup without error 5. A real update check receives HTTP 404 because the draft feed is unpublished. Cloud-library loading, streaming, saved chat/transcript loading, active-row no-restart, video-view position preservation, refresh without losing the paused position, and older-month pagination passed. A one-person hosted SDK session and brief local camera rendering also passed; the camera was turned off and the meeting ended. **These Zoom checks used saved personal developer configuration, not managed Yap sign-in.** The configuration menu and source precedence confirmed that mode. Switching to managed sign-in encountered a Keychain error after clearing prior tokens; the current Zoom UI is disconnected during diagnosis. Managed native authorization, refresh/revocation, a second participant/external meeting, fresh-Mac onboarding and N → N+1 update installation remain pending.

The superseded 0.1.1 installed app passed icon/About, login-item, Help → Report a Bug and recordings chrome checks but failed updater startup with Sparkle error 5. Its account checks had stopped at a protected Keychain prompt; that earlier blocker is resolved in the current 0.1.2 checks above. The local three-page `Yap-SAST-Evidence-v0.1.1.pdf` describes historical 0.1.1 evidence only; the completed exact-source 0.1.2 scan has its own rendered, inspected report.

## Technical Design Overview answers

| Topic | Accurate answer at preparation time | Evidence and limits |
| --- | --- | --- |
| Secure software development lifecycle | **Yes, a documented lightweight process.** If the question specifically requires a certified or independently audited lifecycle, **No**. | [Security policy](../SECURITY.md), source review, least-privilege scope design, locked dependencies, CI, and regression tests. This policy was documented on September 7, 2026; no long-running formal program, certification, independent two-person review, or mandatory branch protection is asserted. |
| Static application security testing (SAST) | **Yes, with the coverage limits below.** The portal choice is entered; the rendered report is prepared locally for upload. | Exact-tag run **34181127659** passed on `01078d5918db5bd8c33e368ec49fce030e4a03be`. CodeQL 2.26.4: Swift **28 rules / 0 results**, JavaScript/TypeScript **103 / 0**, empty analysis errors/warnings. Zero open tag/repository alerts at **03:16:43 UTC on September 8, 2026**. Exact IDs and scope are below. |
| Dynamic application security testing (DAST) | **No completed DAST assessment.** | Synthetic unit/integration tests and manual functional validation are not a deployed-service DAST scan. Public-endpoint and fresh-account authentication acceptance are still separate release checks. If one portal question combines SAST and DAST, explain this distinction; do not attest that both were performed. |
| Periodic independent third-party penetration testing | **No.** | No external penetration-testing provider, recurring schedule, or report exists. Neither CodeQL nor internal tests should be described as an independent penetration test. |

## Saved portal checkpoint

The production Technical Design **Security** section is **3/3** at the operator's latest readback. The TLS answer is **No**, with the HTTP loopback callback, SDK-managed encrypted UDP and unverified Cloudflare minimum-TLS setting explained. The webhook answer is **No / not applicable**; no webhook is implemented. The retention answer is **Yes**, describing local Keychain credentials, caches and user exports separately from transient authentication-service processing.

The **Overview** is now **4/5** after a successful upload and reload readback. The four-face Yap icon is visible in the header, and `Architecture.pdf` and `SSDLC.pdf` remain attached with delete controls. The icon was also applied to dark mode. Initial attachment attempts from Documents stalled; uploading the same authorized files from a temporary submission folder succeeded. The production stack and independent penetration-testing answer **No** remain saved; lightweight SSDLC and SAST are selected **Yes**. The current 0.1.2 scan passed; its SAST report is rendered and prepared locally for attachment, and the separate Zoom App Gallery upload remains pending. The final native PNG screenshots are captured and integrated in the README locally, awaiting GitHub publication. Technical stack, retention and GitHub documentation links are confirmed. The app has not been submitted or approved.

The production Beta authorization URL was regenerated successfully after the listing updates, and the configuration-warning banner disappeared. The native public-client configuration was not changed. This portal readback does not establish native authentication acceptance.

Production browser PKCE and authenticated service checks now pass. Their in-memory test scope and the remaining native acceptance work are recorded below.

## Implemented evidence

| Control | Reviewable source / validation |
| --- | --- |
| Public-client OAuth with PKCE | Native callback state and loopback validation; managed token proxy fixes the public client ID, restricts grant fields and callback format, and rejects duplicate form fields. [Account client](../Sources/YapMeetings/ZoomAccountClient.swift), [service](../Services/zoom-auth/src/index.ts). |
| SDK signing authorization | HMAC-authenticated, expiring authorization binds the exact access-token SHA-256 and public client ID. Signing additionally verifies live access through Zoom's ZAK endpoint. The SDK secret stays in a service secret binding. An arbitrary supplied OAuth token cannot obtain a signature without the matching grant. [Service](../Services/zoom-auth/src/index.ts), [negative and positive tests](../Services/zoom-auth/test/auth.test.ts). |
| Network and input limits | Fixed Zoom upstreams; no upstream redirects; 15-second upstream timeout; bounded request and provider-response bodies; HTTPS-only service endpoints except local tests. Recording loaders validate response types and bounded content, and strip OAuth authorization before a non-Zoom CDN redirect. [Service](../Services/zoom-auth/src/index.ts), [media loader](../Sources/YapAppUI/RecordingMediaLoader.swift). |
| Service retention and abuse controls | No application database or durable store for user credentials or meeting content. The service has no static asset binding; public documentation and Terms are hosted on GitHub. Authentication tokens exist transiently in request processing. Rate-limit keys use hashed source IPs or access tokens: 120 requests/minute and 10 signatures/minute. Explicit application error events are constant; invocation logs and traces are disabled. Cloudflare infrastructure metadata and rate-limit state are separate from application retention. [Deployment configuration](../Services/zoom-auth/wrangler.jsonc). |
| Local storage and content separation | Account credentials use macOS Keychain. Zoom meeting media and recording/chat/transcript downloads go directly between the Mac and Zoom/CDN, not through the signer. User exports and calendar cache have the retention and file protections explained in [Privacy](../PRIVACY.md). |
| Automated regression checks | [PR #7](https://github.com/grinich/yap/pull/7) validation passed **644 Swift tests / 77 suites, 40 Python tests and 24 service tests**, including the real-Sparkle configuration regression and signed-update tampering checks. The 0.1.2 [actual-SDK release run](https://github.com/grinich/yap/actions/runs/34181128184) also passed; public CI uses the SDK-free build. Historical 0.1.1 actual-SDK/local test evidence and the superseded 0.1.0 baseline remain separate in [Releases](Releases.md). |
| Dependency and release controls | `Package.resolved`, npm lockfile and [Zoom SDK lock](../Resources/ZoomSDK.lock.json) pin dependencies. The 0.1.2 release and independent downloads passed Developer ID signatures, app/DMG notarization, stapling, Gatekeeper, hashes and DMG integrity. CryptoKit verified the ZIP and complete signed appcast content; tampered content was rejected. App submission: `23c89640-2a59-4c23-b2b6-f987c6dce891`; DMG: `9f4d357c-db35-4aad-9ed0-9b53cabdfa65`. Native updater startup and personal-mode recording/SDK checks passed; managed sign-in and N → N+1 acceptance remain pending. [Exact current and historical artifacts](Releases.md#current-candidate-evidence). |
| Manual review | Review of PKCE/client-ID boundaries, token-bound signing authorization, live ZAK validation, upstream restrictions, credential redaction, and local-versus-service data flows informed the implementation and regression tests. This is internal code review, not an independent audit. |

## CodeQL configuration and coverage

**Current 0.1.2:** [Exact-tag CodeQL run 34181127659](https://github.com/grinich/yap/actions/runs/34181127659) passed at **03:16:39 UTC on September 8, 2026**, on `refs/tags/v0.1.2`, source `01078d5918db5bd8c33e368ec49fce030e4a03be`. Both analyses used **CodeQL 2.26.4** with `security-extended`; Swift **1738606742: 28 rules / 0 results**, JavaScript/TypeScript **1738537368: 103 rules / 0 results**. Both have empty analysis error and warning fields. Release-tag and repository-wide queries returned **zero open alerts** at **03:16:43 UTC**. These are uploaded rule counts, not counts of security-only queries; zero findings do not establish absence of vulnerabilities.

| Language | Completed job | Analysis record | Created (UTC, September 8, 2026) | Rules | Results |
| --- | --- | --- | --- | --- | --- |
| Swift | [101920296789](https://github.com/grinich/yap/actions/runs/34181127659/job/101920296789) | [1738606742](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738606742) | 03:16:26 | 28 | 0 |
| JavaScript/TypeScript | [101920296643](https://github.com/grinich/yap/actions/runs/34181127659/job/101920296643) | [1738537368](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738537368) | 02:45:59 | 103 | 0 |

The current logs confirm **90/164 Swift files**, **3/3 TypeScript files** and **4/4 GitHub Actions files** scanned. The exact tag has 89 Swift files under `Sources`, `Package.swift` and 74 Swift test files. The SDK-free product scan does not build test targets or cover proprietary SDK/conditional integration paths; Objective-C/Objective-C++ code and deployed runtime behavior are outside its scope. Both SARIFs contain zero results. Nonblocking workflow annotations concern the checkout Node version and supported `tools: latest` alias, separate from the empty analysis warning fields.

**Historical 0.1.1:** The completed [release-tag run](https://github.com/grinich/yap/actions/runs/34171674855) identifies `refs/tags/v0.1.1` and commit `283e37cfddc47cac213be884f5dab6582a8713b0`. Both jobs used CodeQL **2.26.4** and uploaded successful analyses:

| Language | Completed job | Analysis record | Created (UTC) | Rules | Results |
| --- | --- | --- | --- | --- | --- |
| Swift | [101893018571](https://github.com/grinich/yap/actions/runs/34171674855/job/101893018571) | [1738222078](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738222078) | September 8, 2026, 00:27:31 | 28 | 0 |
| JavaScript/TypeScript | [101893018366](https://github.com/grinich/yap/actions/runs/34171674855/job/101893018366) | [1738154457](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738154457) | September 7, 2026, 23:57:23 | 103 | 0 |

These are GitHub's uploaded analysis rule counts, not counts of security-only queries. Both records have empty `error` and `warning` fields. The only workflow annotations were nonblocking notices about the checkout action's Node version and the supported `tools: latest` alias. At **00:28:18 UTC on September 8, 2026**, both release-tag and repository-wide open-alert queries returned empty lists. Zero results do not establish absence of vulnerabilities.

The Swift job selects the repository's Xcode 26.6 toolchain (Swift 6.3.3), initializes CodeQL, then performs a fresh manual arm64 build of the `Yap` product. CodeQL 2.26.2 explicitly added Swift 6.3.3 support. The workflow uses action v4 with current tools so it does not rely on an older bundled compiler extractor. [Exact compiler support](https://codeql.github.com/docs/codeql-overview/codeql-changelog/codeql-cli-2.26.2/), [manual Swift build guidance](https://docs.github.com/en/code-security/reference/code-scanning/codeql/build-options-for-compiled-languages)

JavaScript/TypeScript analysis uses the no-build mode. GitHub supports CodeQL scanning in public repositories; the workflow uploads language-specific results to the repository's code-scanning view. Keep the emitted CodeQL version and analysis logs with the run evidence. [Workflow options](https://docs.github.com/en/code-security/reference/code-scanning/workflow-configuration-options)

The historical 0.1.1 job logs report **90 of 164 Swift files**, **3 of 3 TypeScript files** and **4 of 4 GitHub Actions files** scanned. The exact release tree has 89 Swift files under `Sources`, `Package.swift`, and 74 Swift files under `Tests`; the Swift count is consistent with the product-only build, which does not build the test targets. Do not describe this as a scan of all repository Swift files.

Coverage is limited to what each extractor analyzes. The public Swift build intentionally omits the proprietary Zoom SDK and the conditional SDK bridge path. CodeQL does not support Objective-C/Objective-C++, so the native bridge is not covered by this workflow. Vendor binaries, Apple/Zoom internal behavior, Cloudflare's platform, complete framework-specific behavior, and deployed configuration are not independently assessed by these scans. Manual review and actual-SDK tests remain necessary. [Supported languages and frameworks](https://codeql.github.com/docs/codeql-overview/supported-languages-and-frameworks/)

The earlier main-branch attempt at the same release source, [run 34169878003](https://github.com/grinich/yap/actions/runs/34169878003), was cancelled by workflow concurrency before Swift security queries completed. Its Swift analysis `1738147852` has an execution error and is incomplete. The successful v0.1.1 tag run above replaces that attempt as historical 0.1.1 evidence; the cancelled analysis is not a zero-finding result.

## Production service acceptance

The managed service at `https://meeting-auth.mgrinich.workers.dev` now runs API-only Cloudflare deployment `23757fca-b323-4f65-a8ce-42255449c072`; health returns 200 and `/terms` returns 404. Public documentation and Terms use GitHub. The same authentication logic, including service fix [`9d4e1dd`](https://github.com/grinich/yap/commit/9d4e1dd), passed a live production browser PKCE check at this host on preceding deployment `31a76568-0191-4f73-a3e7-d530fbc52966`. The check passed consent and the loopback callback, token exchange with all three required scopes, one bounded recordings-list page, and an SDK-signature response. Credentials stayed in the test process memory; this check did not write Keychain credentials, initialize the native SDK, exercise the app GUI, or test token refresh or revocation.

The earlier OAuth error **4700** was resolved by regenerating the production Beta authorization URL. A subsequent service failure was reproduced in the actual local Workers runtime: `redirect: "error"` was unsupported. The fix uses `redirect: "manual"` and explicitly rejects upstream 3xx responses. **24 service tests** and an actual local `workerd` before/after regression check passed. This current service validation is separate from the release baseline of 23 service tests.

The three returned scopes were `user:read:zak`, `meeting:write:meeting` and `cloud_recording:read:list_user_recordings`. No credential values or private recordings are included in this evidence. The successful signature response does not establish native SDK authentication, joining or hosting a meeting. Personal-mode recording and a one-person hosted SDK session have since passed as described above. Managed native authorization, second-participant/external meeting, fresh-Mac onboarding, refresh/revocation and uninstall acceptance remain required.

## Complete the submission evidence

1. Record the submitted source commit, app version/build and SHA-256, service deployment version, and verified HTTPS service origin.
2. Attach the rendered, inspected `Yap-SAST-Evidence-v0.1.2.pdf`, with the successful exact-tag run and both analysis records above. Preserve coverage and alert checkpoints; do not substitute the historical `Yap-SAST-Evidence-v0.1.1.pdf` or equate job success with zero vulnerabilities.
3. Attach the actual-SDK test summary and architecture diagram. Use synthetic or redacted evidence without tokens, passcodes or private meeting data.
4. Complete managed native authorization, second-participant/external meeting, fresh-Mac onboarding, token refresh/revocation and uninstall acceptance. Personal-mode recording/SDK checks passed only within the scope above. The live production browser PKCE and authenticated service checks above have passed within their stated scope. HTTPS health and anonymous-signature rejection were also verified. Public Home, Guide, Support, Privacy and Terms URLs point to GitHub; the free-preview Terms are effective September 7, 2026 at the operator’s request. The API service does not serve those documents. Authentication API responses use `no-store`. These functional checks are separate from a DAST assessment.
5. Answer DAST and independent penetration-testing questions according to the current evidence above. If Zoom requires an external assessment, obtain and complete it before asserting compliance.

Historical 0.1.0 and 0.1.1 CI, scans and artifact checks remain recorded for their exact sources. The current 0.1.2 correction has passed its pre-merge test suites, and its actual-SDK release/artifact verification passed. The exact-tag CodeQL scan also passed. SAST-prepared-report upload, managed native authorization, broader acceptance and the gallery remain pending. Complete them before submission. Zoom approval is outstanding; no independent assessment or absence of vulnerabilities is asserted.