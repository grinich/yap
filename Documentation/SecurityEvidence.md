# Yap security evidence for the Zoom review

Updated September 7, 2026 for **Yap 0.1.1, build 2, signed and notarized GitHub draft**, source `283e37cfddc47cac213be884f5dab6582a8713b0`. Its actual-SDK release workflow and main CI passed. Independent downloaded-artifact verification passed at 23:44:30 UTC on September 7, 2026; native installation acceptance remains pending. The immutable **0.1.0, build 1** tag/draft is superseded and must not be installed or promoted. Its historical CI, scan and notarization evidence below belongs to merged main commit `ac394cd9a0834da2d3b58faed5e52efee980d246`. The [architecture diagram](../output/pdf/Zoom-Architecture.pdf) is available as a one-page PDF; its reproducible source is [generate.py](Architecture/generate.py). This is an evidence inventory for Michael Grinich's application, not certification or an independent security assessment. CI and notarization results below are distinct from pending production acceptance and portal submission.

## Current candidate checkpoint

[Main CI 34169878107](https://github.com/grinich/yap/actions/runs/34169878107) and [release run 34169899197](https://github.com/grinich/yap/actions/runs/34169899197) passed at `283e37cfddc47cac213be884f5dab6582a8713b0`, tag `v0.1.1`. The release completed actual-SDK tests, signing, app/DMG notarization, stapling and Gatekeeper checks; all five artifacts are in the unpublished draft. Downloaded hashes, strict signatures, stapled notarization, Gatekeeper and DMG integrity checks passed. CryptoKit independently verified the downloaded ZIP's Ed25519 signature with its bundled public key. This did not verify the entire appcast signature, launch/install the app or complete an update. [Current artifact evidence](Releases.md#current-candidate-evidence).

Current Swift CodeQL is still pending at this checkpoint. The completed historical results below identify their exact earlier commit; they do not establish a completed current-candidate scan or absence of vulnerabilities. Native GUI/Keychain integration, SDK initialization, refresh/revocation, fresh-Mac onboarding and N → N+1 update acceptance remain pending.

## Technical Design Overview answers

| Topic | Accurate answer at preparation time | Evidence and limits |
| --- | --- | --- |
| Secure software development lifecycle | **Yes, a documented lightweight process.** If the question specifically requires a certified or independently audited lifecycle, **No**. | [Security policy](../SECURITY.md), source review, least-privilege scope design, locked dependencies, CI, and regression tests. This policy was documented on September 7, 2026; no long-running formal program, certification, independent two-person review, or mandatory branch protection is asserted. |
| Static application security testing (SAST) | **Yes, with the coverage limits below.** The portal choice is entered; the required evidence upload is outstanding. | [Merged-main CodeQL run 34166962123](https://github.com/grinich/yap/actions/runs/34166962123) passed on `ac394cd9a0834da2d3b58faed5e52efee980d246`. CodeQL **2.26.4** completed Swift analysis with **28 rules / 0 results** and JavaScript/TypeScript with **103 rules / 0 results**, without reported analysis errors or warnings. The repository query returned **zero open code-scanning alerts** at this checkpoint. The [workflow](../.github/workflows/codeql.yml) uses `security-extended` queries. |
| Dynamic application security testing (DAST) | **No completed DAST assessment.** | Synthetic unit/integration tests and manual functional validation are not a deployed-service DAST scan. Public-endpoint and fresh-account authentication acceptance are still separate release checks. If one portal question combines SAST and DAST, explain this distinction; do not attest that both were performed. |
| Periodic independent third-party penetration testing | **No.** | No external penetration-testing provider, recurring schedule, or report exists. Neither CodeQL nor internal tests should be described as an independent penetration test. |

## Saved portal checkpoint

The production Technical Design **Security** section is **3/3** at the operator's latest readback. The TLS answer is **No**, with the HTTP loopback callback, SDK-managed encrypted UDP and unverified Cloudflare minimum-TLS setting explained. The webhook answer is **No / not applicable**; no webhook is implemented. The retention answer is **Yes**, describing local Keychain credentials, caches and user exports separately from transient authentication-service processing.

The **Overview** is **2/5**: the production technical stack is now saved and read back, and independent penetration testing is answered **No**. Lightweight SSDLC and SAST are selected **Yes**, but their required evidence files have not been uploaded. The icon, architecture diagram and security reports are prepared locally but still need to be attached in the portal. The Publish page reports **App Icon, App Gallery and Application Overview** as missing. Technical stack, retention and GitHub documentation links have been saved and confirmed. The representative native gallery capture awaits an unlocked Mac. The app remains **Not ready**, not submitted or approved.

Production browser PKCE and authenticated service checks now pass. Their in-memory test scope and the remaining native acceptance work are recorded below.

## Implemented evidence

| Control | Reviewable source / validation |
| --- | --- |
| Public-client OAuth with PKCE | Native callback state and loopback validation; managed token proxy fixes the public client ID, restricts grant fields and callback format, and rejects duplicate form fields. [Account client](../Sources/YapMeetings/ZoomAccountClient.swift), [service](../Services/zoom-auth/src/index.ts). |
| SDK signing authorization | HMAC-authenticated, expiring authorization binds the exact access-token SHA-256 and public client ID. Signing additionally verifies live access through Zoom's ZAK endpoint. The SDK secret stays in a service secret binding. An arbitrary supplied OAuth token cannot obtain a signature without the matching grant. [Service](../Services/zoom-auth/src/index.ts), [negative and positive tests](../Services/zoom-auth/test/auth.test.ts). |
| Network and input limits | Fixed Zoom upstreams; no upstream redirects; 15-second upstream timeout; bounded request and provider-response bodies; HTTPS-only service endpoints except local tests. Recording loaders validate response types and bounded content, and strip OAuth authorization before a non-Zoom CDN redirect. [Service](../Services/zoom-auth/src/index.ts), [media loader](../Sources/YapAppUI/RecordingMediaLoader.swift). |
| Service retention and abuse controls | No application database or durable store for user credentials or meeting content. The service has no static asset binding; public documentation and Terms are hosted on GitHub. Authentication tokens exist transiently in request processing. Rate-limit keys use hashed source IPs or access tokens: 120 requests/minute and 10 signatures/minute. Explicit application error events are constant; invocation logs and traces are disabled. Cloudflare infrastructure metadata and rate-limit state are separate from application retention. [Deployment configuration](../Services/zoom-auth/wrangler.jsonc). |
| Local storage and content separation | Account credentials use macOS Keychain. Zoom meeting media and recording/chat/transcript downloads go directly between the Mac and Zoom/CDN, not through the signer. User exports and calendar cache have the retention and file protections explained in [Privacy](../PRIVACY.md). |
| Automated regression checks | Current 0.1.1 [main CI 34169878107](https://github.com/grinich/yap/actions/runs/34169878107) and [actual-SDK release run 34169899197](https://github.com/grinich/yap/actions/runs/34169899197) passed at `283e37cfddc47cac213be884f5dab6582a8713b0`. The service fix separately passed **24 tests** and its local Workers-runtime regression check. Public CI uses the SDK-free build; the release supplies the actual-SDK evidence. The superseded 0.1.0 baseline of **644 Swift tests / 77 suites, 39 Python tests and 23 service tests** remains in the [historical record](Releases.md#release-candidate-evidence). |
| Dependency and release controls | `Package.resolved`, npm lockfile with `npm ci --ignore-scripts`, and [Zoom SDK lock](../Resources/ZoomSDK.lock.json) are checked before extraction. The current 0.1.1 [release workflow](https://github.com/grinich/yap/actions/runs/34169899197) passed Developer ID signing, app/DMG notarization, stapling and Gatekeeper checks and uploaded five assets to an unpublished GitHub draft. Downloaded hashes, signatures, stapled notarization, Gatekeeper, DMG integrity and independent ZIP update-signature verification passed. Current app submission: `0e889e8b-488f-43ff-97e8-5d8800edbb18`; DMG: `5f0e64a5-b687-4ca4-b50a-4daab74215c2`. Native installation acceptance remains pending. [Current artifact evidence](Releases.md#current-candidate-evidence). Historical 0.1.0 hashes and notarization IDs remain separate and do not verify 0.1.1. |
| Manual review | Review of PKCE/client-ID boundaries, token-bound signing authorization, live ZAK validation, upstream restrictions, credential redaction, and local-versus-service data flows informed the implementation and regression tests. This is internal code review, not an independent audit. |

## CodeQL configuration and coverage

The Swift job selects the repository's Xcode 26.6 toolchain (Swift 6.3.3), initializes CodeQL, then performs a fresh manual arm64 build of the `Yap` product. CodeQL 2.26.2 explicitly added Swift 6.3.3 support. The workflow uses action v4 with current tools so it does not rely on an older bundled compiler extractor. [Exact compiler support](https://codeql.github.com/docs/codeql-overview/codeql-changelog/codeql-cli-2.26.2/), [manual Swift build guidance](https://docs.github.com/en/code-security/reference/code-scanning/codeql/build-options-for-compiled-languages)

JavaScript/TypeScript analysis uses the no-build mode. GitHub supports CodeQL scanning in public repositories; the workflow uploads language-specific results to the repository's code-scanning view. Keep the emitted CodeQL version and analysis logs with the run evidence. [Workflow options](https://docs.github.com/en/code-security/reference/code-scanning/workflow-configuration-options)

Coverage is limited to what each extractor analyzes. The public Swift build intentionally omits the proprietary Zoom SDK and the conditional SDK bridge path. CodeQL does not support Objective-C/Objective-C++, so the native bridge is not covered by this workflow. Vendor binaries, Apple/Zoom internal behavior, Cloudflare's platform, complete framework-specific behavior, and deployed configuration are not independently assessed by these scans. Manual review and actual-SDK tests remain necessary. [Supported languages and frameworks](https://codeql.github.com/docs/codeql-overview/supported-languages-and-frameworks/)

## Production service acceptance

The managed service at `https://meeting-auth.mgrinich.workers.dev` now runs API-only Cloudflare deployment `23757fca-b323-4f65-a8ce-42255449c072`; health returns 200 and `/terms` returns 404. Public documentation and Terms use GitHub. The same authentication logic, including service fix [`9d4e1dd`](https://github.com/grinich/yap/commit/9d4e1dd), passed a live production browser PKCE check at this host on preceding deployment `31a76568-0191-4f73-a3e7-d530fbc52966`. The check passed consent and the loopback callback, token exchange with all three required scopes, one bounded recordings-list page, and an SDK-signature response. Credentials stayed in the test process memory; this check did not write Keychain credentials, initialize the native SDK, exercise the app GUI, or test token refresh or revocation.

The earlier OAuth error **4700** was resolved by regenerating the production Beta authorization URL. A subsequent service failure was reproduced in the actual local Workers runtime: `redirect: "error"` was unsupported. The fix uses `redirect: "manual"` and explicitly rejects upstream 3xx responses. **24 service tests** and an actual local `workerd` before/after regression check passed. This current service validation is separate from the release baseline of 23 service tests.

The three returned scopes were `user:read:zak`, `meeting:write:meeting` and `cloud_recording:read:list_user_recordings`. No credential values or private recordings are included in this evidence. The successful signature response does not establish native SDK authentication, joining or hosting a meeting. Fresh-Mac installation, native account integration, refresh/revocation, and the remaining recording playback/uninstall acceptance checks are still required.

## Complete the submission evidence

1. Record the submitted source commit, app version/build and SHA-256, service deployment version, and verified HTTPS service origin.
2. Link the successful CI and both CodeQL analysis runs for that commit. Review and record any findings and resolutions; do not equate job success with zero vulnerabilities.
3. Attach the actual-SDK test summary and architecture diagram. Use synthetic or redacted evidence without tokens, passcodes or private meeting data.
4. Complete fresh-Mac/native account integration, token refresh and revocation, recording playback and uninstall acceptance for the production configuration. The live production browser PKCE and authenticated service checks above have passed within their stated scope. HTTPS health and anonymous-signature rejection were also verified. Public Home, Guide, Support, Privacy and Terms URLs point to GitHub; the free-preview Terms are effective September 7, 2026 at the operator’s request. The API service does not serve those documents. Authentication API responses use `no-store`. These functional checks are separate from a DAST assessment.
5. Answer DAST and independent penetration-testing questions according to the current evidence above. If Zoom requires an external assessment, obtain and complete it before asserting compliance.

The completed [historical CodeQL baseline](https://github.com/grinich/yap/actions/runs/34166962123) identifies 0.1.0 source `ac394cd9a0834da2d3b58faed5e52efee980d246`, with the SDK/Objective-C coverage limits above. The current 0.1.1 CI and release packaging results are separate; current Swift CodeQL remains pending at this checkpoint, while downloaded-artifact verification passed within the scope recorded above. Complete the remaining native acceptance and required portal uploads before submission. Zoom approval is outstanding; no independent assessment or absence of vulnerabilities is asserted.
