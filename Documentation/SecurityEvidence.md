# Yap security evidence for the Zoom review

Updated September 7, 2026 (Pacific), for **Yap 0.1.3, build 4**, source `b920870e4a4a1f53b31d6f73c818c5aa2f75e5cf`. Its actual-SDK packaging, independent downloaded-artifact verification, exact-tag CodeQL and scoped native managed-account checks passed. The candidate remains an unpublished draft while Zoom review is prepared. This is Michael Grinich's evidence inventory, not certification or an independent security assessment. CI, scans, notarization, native acceptance and provider approval are separate claims. Superseded versions remain in the [historical release evidence](Releases.md#superseded-candidate-v012).

## Current candidate checkpoint

[PR #10 CI 34185005400](https://github.com/grinich/yap/actions/runs/34185005400) passed **647 Swift tests / 77 suites, 40 Python tests and 24 authorization-service tests**. The isolated actual-SDK suite also passed 647 tests / 77 suites. The correction atomically removes active Zoom credentials/configuration, attempts all scoped legacy cleanup and reconciles UI state after a denied cleanup. Failed vault writes leave active records intact; Google credentials, tombstones and existing Keychain protections are preserved.

The actual-SDK [release run 34185478572](https://github.com/grinich/yap/actions/runs/34185478572) passed and created unpublished draft **384441843** with five assets. Independent downloaded verification passed at **04:20:13 UTC on September 8, 2026**: all five GitHub digests, ZIP/DMG checksum files, app/DMG Developer ID signatures, stapled notarization, Gatekeeper and DMG integrity. CryptoKit independently verified the ZIP and complete signed appcast content with the bundled public key, checked content length and trailing content, and rejected in-memory tampering. The downloaded bundle is **0.1.3, build 4**, `com.grinich.yap`, signed by team `VSVHNQP588`, with the workers.dev endpoint, release feed and both Sparkle verification prerequisites enabled. App notarization was Accepted under `43bbd9f1-2025-46f2-b422-50c6eafe7a30`; DMG under `5954c923-c9fb-4562-ab4b-ff92d99c7d21`. Artifact verification did not launch the app or prove update delivery. [Current hashes and exact timestamps](Releases.md#current-candidate-evidence).

[Exact-tag CodeQL run 34185481943](https://github.com/grinich/yap/actions/runs/34185481943) passed at **04:33:08 UTC on September 8, 2026**, on `refs/tags/v0.1.3`, source `b920870e4a4a1f53b31d6f73c818c5aa2f75e5cf`. Both analyses used **CodeQL 2.26.4** with `security-extended`: Swift **1738803023: 28 rules / 0 results**; JavaScript/TypeScript **1738719791: 103 rules / 0 results**. Both analysis error and warning fields are empty. Release-tag and repository-wide queries returned **zero open alerts** at **04:33:54 UTC**. These are uploaded rule counts, not counts of security-only queries; zero findings do not establish absence of vulnerabilities.

The two-page **`Yap-SAST-Evidence-v0.1.3.pdf`** is rendered, visually inspected and staged locally for private portal upload: **9,261 bytes**, SHA-256 `cb126a3756a5f57b85f71483c0c321de65313b846e8779bd1adeac9468778a81`. It covers the exact scan, current artifacts and the separate native acceptance scope. A fresh browser-tab server readback confirmed this exact filename replaced the historical 0.1.2 attachment. The PDF has no public download URL.

## Native acceptance

The release operator separately installed the independently verified, downloaded **0.1.3 (build 4)** normally. Startup had no updater error; Google and selected calendars persisted, and Zoom remained connected in **managed mode**. Real cloud video, automatic chat presentation and an actual timestamped transcript loaded. Space paused at 12 seconds, S selected 1.25× and Right moved to 22 seconds; V changed the view while preserving that position, pause and speed. F entered and exited fullscreen. A managed one-person hosted meeting started with microphone muted and camera off, and normal End for Everyone returned to the agenda. About showed Yap 0.1.3 (4) and the clean four-face icon; the login switch was turned on and restored to its original off state. Help → Report a Bug opened the GitHub issue-template chooser without creating an issue. Both accounts ended connected with no active meeting.

The preceding local optimized build at `66dac66` separately passed personal-to-managed switching, local disconnect/account-state clearing, production PKCE reauthorization, restart and Google preservation. macOS still reported denial of protected legacy-item cleanup; the active-vault change and UI state now completed correctly without bypassing Keychain protections. Local disconnect is not provider-side revocation. **Fresh-Mac onboarding, multiuser/external meetings, live token refresh, provider-side revocation and N → N+1 update installation remain unverified.**

## Technical Design Overview answers

| Topic | Accurate answer | Evidence and limits |
| --- | --- | --- |
| Secure software development lifecycle | **Yes, a documented lightweight process.** If the question specifically requires a certified or independently audited lifecycle, **No**. | [Security policy](../SECURITY.md), internal source review, least-privilege scopes, locked dependencies, CI and regressions. Adopted September 7, 2026; no formal certification, independent two-person review or mandatory branch protection is asserted. |
| Static application security testing (SAST) | **Yes, within the coverage below.** | Exact-tag run **34185481943** passed with both uploaded analyses; the current PDF is ready locally. Zero findings are not proof of no vulnerabilities. |
| Dynamic application security testing (DAST) | **No completed DAST assessment.** | Unit/integration tests and manual functional checks are not a deployed-service DAST scan. If one portal question combines SAST and DAST, explain the distinction. |
| Periodic independent third-party penetration testing | **No.** | No external provider, recurring schedule or report exists. CodeQL and internal tests are not independent penetration testing. |

## Saved portal checkpoint

The release operator confirmed the four-face Yap icon, including dark mode, the architecture and SSDLC attachments, and the separate Zoom App Gallery after reload. Technical Design shows **Overview 5/5**, **Security 3/3**, and Publish shows **Ready**. Reviewer notes and manual activation are prepared. The current 0.1.3 SAST PDF, architecture and SSDLC attachments persisted in a fresh browser-tab server readback. Private reviewer delivery is verified against the exact DMG checksum, with access expiring October 8, 2026 at 04:29:19 UTC. The review package is complete and submission is being prepared; **the app has not been submitted or approved**. Ready is the portal's completion indicator, not approval. The selected initial audience excludes the EU. The production Beta authorization URL was regenerated successfully and its warning cleared; native public-client configuration was unchanged.

The saved Security answers are TLS **No** (HTTP loopback, SDK-managed encrypted UDP and unverified Cloudflare minimum-TLS setting explained), webhooks **No / not applicable**, and retention **Yes** (local Keychain/cache/exports distinguished from transient service processing). The technical stack and GitHub documentation links are saved. SSDLC/SAST are selected **Yes** and independent penetration testing **No**. The [architecture PDF](../output/pdf/Zoom-Architecture.pdf) has reproducible [source](Architecture/generate.py).

The final native PNG gallery, agenda and recording screenshots are published in the README through [PR #9](https://github.com/grinich/yap/pull/9); their [capture provenance](Images/README.md) distinguishes sample content from live private meetings. Google consent is **Yap**; its desktop client and project display names are **Yap for Mac**. The current Zoom development and production identity is **Yap**. Google remains **Testing**, without public verification approval or a new public rollout decision. [Provider branding checkpoint](Distribution.md#provider-branding-checkpoint).

## Implemented evidence


| Control | Reviewable source / validation |
| --- | --- |
| Public-client OAuth with PKCE | Native callback state and loopback validation; managed token proxy fixes the public client ID, restricts grant fields and callback format, and rejects duplicate form fields. [Account client](../Sources/YapMeetings/ZoomAccountClient.swift), [service](../Services/zoom-auth/src/index.ts). |
| SDK signing authorization | HMAC-authenticated, expiring authorization binds the exact access-token SHA-256 and public client ID. Signing additionally verifies live access through Zoom's ZAK endpoint. The SDK secret stays in a service secret binding. An arbitrary supplied OAuth token cannot obtain a signature without the matching grant. [Service](../Services/zoom-auth/src/index.ts), [negative and positive tests](../Services/zoom-auth/test/auth.test.ts). |
| Network and input limits | Fixed Zoom upstreams; no upstream redirects; 15-second upstream timeout; bounded request and provider-response bodies; HTTPS-only service endpoints except local tests. Recording loaders validate response types and bounded content, and strip OAuth authorization before a non-Zoom CDN redirect. [Service](../Services/zoom-auth/src/index.ts), [media loader](../Sources/YapAppUI/RecordingMediaLoader.swift). |
| Service retention and abuse controls | No application database or durable store for user credentials or meeting content. The service has no static asset binding; public documentation and Terms are hosted on GitHub. Authentication tokens exist transiently in request processing. Rate-limit keys use hashed source IPs or access tokens: 120 requests/minute and 10 signatures/minute. Explicit application error events are constant; invocation logs and traces are disabled. Cloudflare infrastructure metadata and rate-limit state are separate from application retention. [Deployment configuration](../Services/zoom-auth/wrangler.jsonc). |
| Local storage and content separation | Account credentials use macOS Keychain. Zoom meeting media and recording/chat/transcript downloads go directly between the Mac and Zoom/CDN, not through the signer. User exports and calendar cache have the retention and file protections explained in [Privacy](../PRIVACY.md). |
| Automated regression checks | [PR #10 CI 34185005400](https://github.com/grinich/yap/actions/runs/34185005400) passed **647 Swift tests / 77 suites, 40 Python tests and 24 service tests**. The isolated actual-SDK suite and [0.1.3 release run](https://github.com/grinich/yap/actions/runs/34185478572) passed. Regressions include atomic credential transitions/denial reconciliation, real-Sparkle startup configuration and signed-update tampering. |
| Dependency and release controls | `Package.resolved`, npm lockfile and [Zoom SDK lock](../Resources/ZoomSDK.lock.json) pin dependencies. The current release and independent downloads passed Developer ID signatures, app/DMG notarization, stapling, Gatekeeper, hashes and DMG integrity. Both ZIP and complete signed appcast content were independently verified; tampering was rejected. Separate native startup and managed-account checks passed; N → N+1 delivery remains unverified. [Exact current and historical artifacts](Releases.md#current-candidate-evidence). |
| Manual review | Review of PKCE/client-ID boundaries, token-bound signing authorization, live ZAK validation, upstream restrictions, credential redaction, and local-versus-service data flows informed the implementation and regression tests. This is internal code review, not an independent audit. |

## CodeQL configuration and coverage

[Exact-tag CodeQL run 34185481943](https://github.com/grinich/yap/actions/runs/34185481943) passed at **04:33:08 UTC on September 8, 2026**, on `refs/tags/v0.1.3`, source `b920870e4a4a1f53b31d6f73c818c5aa2f75e5cf`. Both analyses used **CodeQL 2.26.4** with `security-extended`: Swift **1738803023: 28 rules / 0 results**; JavaScript/TypeScript **1738719791: 103 rules / 0 results**. Both analysis error and warning fields are empty. Release-tag and repository-wide queries returned **zero open alerts** at **04:33:54 UTC**. These are uploaded rule counts, not counts of security-only queries; zero findings do not establish absence of vulnerabilities.

| Language | Completed job | Analysis record | Created (UTC, September 8, 2026) | Rules | Results |
| --- | --- | --- | --- | --- | --- |
| Swift | [101932853912](https://github.com/grinich/yap/actions/runs/34185481943/job/101932853912) | [1738803023](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738803023) | 04:32:52 | 28 | 0 |
| JavaScript/TypeScript | [101932854152](https://github.com/grinich/yap/actions/runs/34185481943/job/101932854152) | [1738719791](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738719791) | 04:02:05 | 103 | 0 |

The downloaded job logs confirm **90/164 Swift files**, **3/3 TypeScript files** and **4/4 GitHub Actions files** scanned. The exact tag contains 89 Swift files under `Sources`, `Package.swift` and 74 Swift test files. The SDK-free product build does not build test targets or cover proprietary SDK/conditional integration paths. Objective-C/Objective-C++ and deployed runtime behavior are outside the scan. Both downloaded SARIFs contain zero results. Nonblocking workflow notices concern the checkout Node version and supported `tools: latest` alias, separate from the empty analysis warning fields.

The Swift job uses Xcode 26.6 / Swift 6.3.3 for a manual arm64 product build; JavaScript/TypeScript uses no-build mode. [Compiler support](https://codeql.github.com/docs/codeql-overview/codeql-changelog/codeql-cli-2.26.2/), [manual Swift builds](https://docs.github.com/en/code-security/reference/code-scanning/codeql/build-options-for-compiled-languages), [supported languages and frameworks](https://codeql.github.com/docs/codeql-overview/supported-languages-and-frameworks/).

### Historical 0.1.2 scan

The superseded tag `v0.1.2`, source `01078d5918db5bd8c33e368ec49fce030e4a03be`, passed [run 34181127659](https://github.com/grinich/yap/actions/runs/34181127659) at **03:16:39 UTC on September 8, 2026**. CodeQL 2.26.4 with `security-extended` uploaded Swift analysis **1738606742** (job **101920296789**, created **03:16:26 UTC**), **28 rules / 0 results**, and JavaScript/TypeScript analysis **1738537368** (job **101920296643**, created **02:45:59 UTC**), **103 / 0**. Both errors/warnings were empty; open tag/repository alerts were zero at **03:16:43 UTC**. Logs reported 90/164 Swift, 3/3 TypeScript and 4/4 Actions files; both SARIFs had zero results. The historical two-page `Yap-SAST-Evidence-v0.1.2.pdf` remains unchanged: **9,057 bytes**, SHA-256 `ffea8f3ab5b34723e9c783bdfc17bac6869d0fa3a8f8ef3d0ffb5e7aad6412f3`.

These results apply only to the superseded candidate that exposed the Keychain transition bug. Its personal-mode native checks are not evidence of managed sign-in. [Historical package/native evidence](Releases.md#superseded-candidate-v012).

### Historical 0.1.1 scan

**Historical 0.1.1:** The completed [release-tag run](https://github.com/grinich/yap/actions/runs/34171674855) identifies `refs/tags/v0.1.1` and commit `283e37cfddc47cac213be884f5dab6582a8713b0`. Both jobs used CodeQL **2.26.4** and uploaded successful analyses:

| Language | Completed job | Analysis record | Created (UTC) | Rules | Results |
| --- | --- | --- | --- | --- | --- |
| Swift | [101893018571](https://github.com/grinich/yap/actions/runs/34171674855/job/101893018571) | [1738222078](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738222078) | September 8, 2026, 00:27:31 | 28 | 0 |
| JavaScript/TypeScript | [101893018366](https://github.com/grinich/yap/actions/runs/34171674855/job/101893018366) | [1738154457](https://api.github.com/repos/grinich/yap/code-scanning/analyses/1738154457) | September 7, 2026, 23:57:23 | 103 | 0 |

These are GitHub's uploaded analysis rule counts, not counts of security-only queries. Both records have empty `error` and `warning` fields. The only workflow annotations were nonblocking notices about the checkout action's Node version and the supported `tools: latest` alias. At **00:28:18 UTC on September 8, 2026**, both release-tag and repository-wide open-alert queries returned empty lists. Zero results do not establish absence of vulnerabilities.

The Swift job selects the repository's Xcode 26.6 toolchain (Swift 6.3.3), initializes CodeQL, then performs a fresh manual arm64 build of the `Yap` product. CodeQL 2.26.2 explicitly added Swift 6.3.3 support. The workflow uses action v4 with current tools so it does not rely on an older bundled compiler extractor. [Exact compiler support](https://codeql.github.com/docs/codeql-overview/codeql-changelog/codeql-cli-2.26.2/), [manual Swift build guidance](https://docs.github.com/en/code-security/reference/code-scanning/codeql/build-options-for-compiled-languages)

JavaScript/TypeScript analysis uses the no-build mode. GitHub supports CodeQL scanning in public repositories; the workflow uploads language-specific results to the repository's code-scanning view. Keep the emitted CodeQL version and analysis logs with the run evidence. [Workflow options](https://docs.github.com/en/code-security/reference/code-scanning/workflow-configuration-options)

The historical 0.1.1 job logs report **90 of 164 Swift files**, **3 of 3 TypeScript files** and **4 of 4 GitHub Actions files** scanned. The exact release tree has 89 Swift files under `Sources`, `Package.swift`, and 74 Swift files under `Tests`; the Swift count is consistent with the product-only build, which does not build the test targets. Do not describe this as a scan of all repository Swift files.

Coverage is limited to what each extractor analyzes. The public Swift build intentionally omits the proprietary Zoom SDK and the conditional SDK bridge path. CodeQL does not support Objective-C/Objective-C++, so the native bridge is not covered by this workflow. Vendor binaries, Apple/Zoom internal behavior, Cloudflare's platform, complete framework-specific behavior, and deployed configuration are not independently assessed by these scans. Manual review and actual-SDK tests remain necessary. [Supported languages and frameworks](https://codeql.github.com/docs/codeql-overview/supported-languages-and-frameworks/)

The earlier main-branch attempt at the same release source, [run 34169878003](https://github.com/grinich/yap/actions/runs/34169878003), was cancelled by workflow concurrency before Swift security queries completed. Its Swift analysis `1738147852` has an execution error and is incomplete. The successful v0.1.1 tag run above replaces that attempt as historical 0.1.1 evidence; the cancelled analysis is not a zero-finding result.

## Production service acceptance


The managed service at `https://meeting-auth.mgrinich.workers.dev` now runs API-only Cloudflare deployment `23757fca-b323-4f65-a8ce-42255449c072`; health returns 200 and `/terms` returns 404. Public documentation and Terms use GitHub. The same authentication logic, including service fix [`9d4e1dd`](https://github.com/grinich/yap/commit/9d4e1dd), passed a live production browser PKCE check at this host on preceding deployment `31a76568-0191-4f73-a3e7-d530fbc52966`. The check passed consent and the loopback callback, token exchange with all three required scopes, one bounded recordings-list page, and an SDK-signature response. Credentials stayed in the test process memory; this check did not write Keychain credentials, initialize the native SDK, exercise the app GUI, or test token refresh or revocation.

The earlier OAuth error **4700** was resolved by regenerating the production Beta authorization URL. A subsequent service failure was reproduced in the actual local Workers runtime: `redirect: "error"` was unsupported. The fix uses `redirect: "manual"` and explicitly rejects upstream 3xx responses. **24 service tests** and an actual local `workerd` before/after regression check passed. This current service validation is separate from the release baseline of 23 service tests.

The three returned scopes were `user:read:zak`, `meeting:write:meeting` and `cloud_recording:read:list_user_recordings`. No credential values or private recordings are included in this evidence. The successful signature response does not establish native SDK authentication, joining or hosting a meeting. Managed native reauthorization and downloaded 0.1.3 recording/solo-host acceptance have since passed separately within the [recorded native scope](Releases.md#native-acceptance). Multiuser/external meetings, fresh-Mac onboarding, live refresh/provider revocation and uninstall acceptance remain unverified.

## Complete the submission evidence

1. Record the immutable submitted tag/source, app version/build and SHA-256, service deployment and verified HTTPS origin. Use the current 0.1.3 artifacts, not a superseded package.
2. The rendered, inspected `Yap-SAST-Evidence-v0.1.3.pdf` is attached, with exact run/analysis IDs, coverage and alert checkpoint. Preserve earlier PDFs as history; no scan proves absence of vulnerabilities.
3. The private reviewer installer is verified against the exact current DMG; supply it only through the authorized reviewer channel with the actual-SDK/native test scope and saved architecture/SSDLC documents. Use synthetic or redacted evidence without tokens, passcodes or private meeting content.
4. Complete or explicitly disclose remaining multiuser/external-meeting, fresh-Mac, live refresh/provider-revocation, uninstall and N → N+1 checks. Local disconnect and functional service checks are not provider revocation or DAST.
5. Keep DAST and independent penetration-testing answers consistent with the evidence. Obtain any additional assessment Zoom requires before asserting compliance.

The current package, scan, scoped native acceptance and published native README screenshots are complete. The current SAST attachment is confirmed and the private reviewer installer matches the verified DMG. The review package is complete; submission is being prepared and Zoom approval remains outstanding.
