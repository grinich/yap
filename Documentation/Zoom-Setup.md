# Connect Yap to real Zoom meetings

## Packaged app: managed sign-in

The official app includes its Zoom connection configuration. On an Apple silicon Mac with macOS 26 or newer, install the exact candidate supplied for testing, open **Yap → Settings → Connections**, and choose **Sign in to Zoom**. Complete Zoom's browser consent with your own account and return to Yap. Normal users and reviewers do not need to create a developer app, enter a client secret, or import a configuration file. Google Calendar is optional and uses its existing separate sign-in flow.

**Version boundary:** released **Yap 0.1.6 (build 7)** still uses the previous managed public-client OAuth flow and local HTTP callback. The revised HTTPS production-client flow described below is implemented in source for an **upcoming review candidate**, not in those immutable release bytes. It requires a newly packaged version/build, a matching service deployment and saved Zoom configuration, and live browser/native acceptance. Source changes, deployment, Apple signing and Zoom approval are separate checkpoints. See the [0.1.6 handoff](ZoomReview-0.1.6.md) and [end-to-end test plan](Zoom-Test-Plan.md).

| Connection | OAuth identity and callback | Secret location |
| --- | --- | --- |
| Upcoming managed candidate | Production OAuth client `UHoml3aIQpy86gZeijjfpQ`; fixed HTTPS callback `https://meeting-auth.mgrinich.workers.dev/oauth/zoom/callback` | Production OAuth/SDK secret stays on the authorization service |
| Released 0.1.6 managed connection | Public OAuth client `_Xz_EnBNS3OPtUmqZ1og3A`; temporary `http://127.0.0.1:<port>/callback` listener, followed by service exchange | SDK secret stays on the service; public-client OAuth does not use a client secret |
| Personal developer mode | Owner's public OAuth client and temporary loopback listener; direct exchange with Zoom | Owner's SDK secret is stored in the Mac's Keychain for local SDK signing |

These identifiers are public configuration, not login credentials. A managed account grant for the previous public client does not become a production-client grant automatically; the new candidate requires explicit sign-in for its configured client. If a personal developer override is active, **Developer configuration… → Use Yap sign-in** removes that override before using the packaged connection. The official app does not require sharing another person's SDK secret.

## Upcoming managed authorization flow

1. The Mac generates a random sign-in nonce and PKCE verifier, keeps the pending attempt in memory, and posts them over HTTPS to `/v1/oauth/session` on the signed app's fixed service origin.
2. The service returns a Zoom authorization URL using the fixed production client ID, S256 challenge and HTTPS callback. Its authenticated encrypted state binds the nonce, verifier, production audience, callback and an expiry of at most 180 seconds. The app validates the returned URL before opening the browser.
3. Zoom sends the authorization code to that HTTPS callback. The service validates the state and exchanges the code with Zoom using the production OAuth secret and verifier. The service never asks a native custom URL handler to exchange a raw production authorization code.
4. The browser completion page offers **Open Yap**, carrying the original nonce and an authenticated encrypted token handoff at `yap://oauth/zoom`. This custom URL contains no plaintext OAuth code, verifier or token. It is a return to the app, not the registered Zoom callback.
5. Yap accepts a response only for its pending nonce and consumes that pending response once. It redeems the handoff over HTTPS at `/v1/oauth/token` with the original verifier. The service checks the encrypted audience/nonce, verifier proof and expiry, then returns tokens with their original remaining lifetime. Yap validates own-user ZAK access and saves the account tokens/signing grant in Keychain before reporting a successful connection.
6. Token refresh uses the service's confidential production-client exchange. To authorize the Meeting SDK, the app separately supplies its access token and token-bound signing grant; the service checks current authorization with Zoom's own-user ZAK endpoint before issuing the SDK JWT. Meeting creation, recordings and media continue between the Mac and Zoom; Google authentication is unchanged.

The new managed flow opens no local HTTP OAuth listener. Session state and the token handoff use distinct-purpose AES-GCM envelopes, each expiring within 180 seconds, derived from the service's signing-grant secret. The service keeps no persistent user/token/session database; plaintext credentials are processed in request memory. Encrypted envelopes may remain in browser history, and expiry does not erase that history. The desktop nonce is consumed once, but the stateless service does not store redemption records; a handoff plus its original verifier can be replayed within its short validity window. Do not describe this as server-enforced single-use redemption.

The service requires its production OAuth and SDK client IDs to match before using the same server secret for both functions. The native bundle contains only public identifiers and the service address. The service retains a separate legacy public-client exchange/refresh path so the already released 0.1.6 managed installer can continue using its original configuration. [Native managed flow](../Sources/YapMeetings/ZoomManagedOAuth.swift), [account client](../Sources/YapMeetings/ZoomAccountClient.swift), [service configuration and routes](../Services/zoom-auth/README.md), [Privacy](../PRIVACY.md).

All current Zoom flows use the user-level scopes `user:read:zak`, `meeting:write:meeting`, and `cloud_recording:read:list_user_recordings`. These authorize the signed-in user's SDK identity, explicit instant-meeting creation and own-user cloud-recording library. SDK features such as live chat, sharing and recording controls use meeting permissions rather than additional REST scopes. There are no app-owned user roles or separate Yap login credentials. Use the [test plan](Zoom-Test-Plan.md) to prepare actual account entitlements and synthetic recordings, and to verify each scope with expected outcomes.

The upcoming `/connect` integration page offers **Connect Zoom in Yap**, which opens `yap://connect/zoom`; the app starts sign-in or shows its existing connection settings. Test this only with a build that implements that route. It is not a working new entry path for the unchanged 0.1.6 installer merely because the webpage exists. The page, callback, native return, cancellation, wrong/expired state, refresh and reauthorization all need validation against the final packaged candidate.

## Historical native checkpoints

The dated checks in this section and the later native implementation/permission notes are retained historical evidence. They do not validate the upcoming managed OAuth flow, replace its live tests, or establish Zoom approval.

**Yap identity update, September 7, 2026:** the bundle ID is `com.grinich.yap`; see [Bundle Identity](Bundle-Identity.md) for the current migration. Recorded live checks below predate the Yap rename and retain their historical identity and permission boundaries.

**Historical installed checkpoint — September 6, 2026:** 247 automated tests passed. The final Developer ID build loaded saved accounts and hosted without permission prompts. A native Zoom second endpoint received the selected animated test window; minimizing presented the PiP, handle, and chat. Stop sharing ended the receiver share, restored the main window onscreen, and removed chat. Both endpoints then left normally and companion panels closed. Native synthetic QA verified drag and pointer fade. Persistent physical foreground activation under CUA, repeated live renderer transitions, and full-display panel exclusion remained unverified. The 200 ms renderer handoff passed native fixtures; this final live sequence logged detach-pending but did not log handoff-preserved. See [sharing companions](Sharing-Companions.md) for the dated evidence. Earlier records below retain their original test boundaries.

**Historical integration status:** the official Apple silicon Meeting SDK **7.1.5 (84750)** was downloaded under `Vendor/Zoom/zoom-sdk-macos-7.1.5.84750/ZoomSDK`. The Objective-C bridge and Swift `ZoomMeetingDriver` compiled against those actual headers, and the SDK-enabled app launched.

Historical personal-mode user consent for public-client PKCE OAuth with `user:read:zak` and `meeting:write:meeting` completed, and ZAK retrieval succeeded; this was not Marketplace approval. Yap created and entered an own-account meeting as host: native authentication and start returned success, followed by AudioReady and InMeeting within approximately 3.3 seconds. Entry preserved microphone-muted and camera-off state. On September 6 at 10:13, a separate native Zoom endpoint sent camera video that Yap visibly rendered; SDK statistics confirmed an incoming 320 × 180 stream. Incoming frames recovered after camera off/on and survived chat resizing. The duplicate native SDK name/mute decoration was also visibly removed. See [live video evidence](Video-Runtime.md) for the exact observed quality and remaining tests. Mock participant counts cannot establish a live-video subscription limit.

## SDK package for source builds only

Zoom distributes current macOS SDK packages through its authenticated Marketplace. In a General app, enable **Features → Embed → Meeting SDK**, select macOS, and download the current Apple silicon package. Save the downloaded package into `Yap/Vendor/Zoom/` and keep its original directory structure. The public `zoom-sdk-macos` repository points to Marketplace for current downloads; its old releases are not a substitute for a supported SDK. [Download instructions](https://developers.zoom.us/docs/meeting-sdk/macos/get-started/), [official repository](https://github.com/zoom/zoom-sdk-macos).

Do not paste credentials into source files, commit vendor secrets, or copy private frameworks out of the installed Zoom client. Yap's personal build imports the owner's development credentials into this Mac's Keychain and signs the short-lived SDK JWT at runtime. That owner-only path is unsuitable for distributing shared developer credentials; public distribution needs a trusted signing service. Never bundle the client secret in the app. [Meeting SDK credentials](https://developers.zoom.us/docs/meeting-sdk/get-credentials/).

## Legacy personal developer setup

This owner-only configuration is retained for development. It is not required for the official managed app and is not the setup for the upcoming production HTTPS callback.

There are separate layers: SDK application authentication, the human's Zoom identity, and permission to enter a particular meeting. Yap uses the human identity/ZAK path, including host access, rather than a bot identity. An SDK JWT alone does not provide a user's hosting authority. Meetings outside the developer account require the applicable Zoom app review and authorization even when Yap is only for personal use. Test an account-owned meeting first, then externally hosted meetings once authorized. [Authorization FAQ](https://developers.zoom.us/docs/meeting-sdk/obf-faq/).

### Personal public-client configuration

For this legacy personal path only, use the owner's user-managed General app with **Use Public Client OAuth**, register `http://127.0.0.1/callback`, and add `user:read:zak`, `meeting:write:meeting`, and `cloud_recording:read:list_user_recordings`. The meeting-write scope creates a meeting for the signed-in user on **Start new meeting**; the recording scope enables that user's library. No administrator scopes are requested. The legacy native PKCE flow uses an ephemeral loopback port with the registered host/path, a fresh state and S256 verifier. It separately uses the owner's SDK Client ID and Client Secret for local SDK signing. Do not configure the new confidential production client to use this HTTP loopback path. [Native OAuth](https://developers.zoom.us/docs/integrations/oauth/), [Meetings API](https://developers.zoom.us/docs/api/meetings/).

In Yap Settings, choose **Enter Zoom configuration…**, enter the regular **Client ID**, paste the **Client Secret** into its native secure field, and enter the **Public OAuth Client ID**. Save configuration writes directly to a non-synchronizing, unlocked-device-only Keychain item. The secret field clears immediately on Save and all fields clear on dismissal. Existing configuration can be replaced from its settings menu; replacement disconnects the saved Zoom authorization and requires sign-in again. Configuration changes are disabled during calls and connection mutations.

JSON import remains available. The schema below uses labels, not actual credentials; manual entry avoids creating an additional plaintext credential file.

```json
{
  "sdkClientID": "REGULAR_CLIENT_ID_FROM_ZOOM",
  "sdkClientSecret": "CLIENT_SECRET_FROM_ZOOM",
  "oauthPublicClientID": "PUBLIC_CLIENT_ID_FROM_ZOOM"
}
```

`ZoomAccountClient.configure(fromJSON:)` imports this configuration. In **personal mode**, `connect(openURL:)` opens the system browser, verifies the loopback response, exchanges the code directly with Zoom, validates ZAK access, and saves rotating OAuth credentials. `meetingCredentials()` obtains a fresh five-minute ZAK from `/v2/users/me/zak` immediately before joining and makes a one-hour native SDK JWT locally. `hostingCredentials(title:)` also creates an own-user instant meeting via `POST /v2/users/me/meetings` with `type: 1` and returns its numeric ID alongside fresh SDK credentials. `disconnect()` removes local OAuth credentials; `removeConfiguration()` also removes the imported developer settings. Disconnect does not claim to revoke Zoom's server-side app authorization. [Authenticated-user ZAK endpoint](https://developers.zoom.us/docs/api/users/), [native SDK JWT format](https://developers.zoom.us/docs/meeting-sdk/auth/), [Meetings API](https://developers.zoom.us/docs/api/meetings/).

The created room uses host and participant video off, mute upon entry, waiting room on, join before host off, and no personal meeting ID. Automatic recording, meeting summary, and AI Companion questions are explicitly off. Yap sends no invitations. The prepared room ID is reused across retries until the SDK confirms InMeeting; cancellation after a confirmed creation retains that ID. An uncertain creation result blocks another POST until an explicit reconnect, avoiding duplicate meetings. Missing creation permission produces a reconnect/setup error instead of widening authorization silently.

The **legacy loopback** callback displays a self-contained Yap completion page with the app icon, light/dark appearance, reduced-motion support, and an explicit **Open Yap** button. That legacy page clears the authorization query from the address bar. It does not claim the connection is complete before token exchange: Yap comes to the foreground only after the token exchange, ZAK check, and credential save succeed. Its button uses the parameter-free `yap://open` route, which only reveals the main window. These local callback pages load no external assets, prohibit caching, and use a nonce-based Content Security Policy. The upcoming managed HTTPS completion page and encrypted `yap://oauth/zoom` handoff are a separate implementation described above.

Token-bearing HTTP requests refuse redirects and use an ephemeral session. Error messages omit response bodies and token values. OAuth/token/meeting-creation unit tests use fixtures. Separately, real account authorization, ZAK retrieval, SDK host startup, and incoming camera video from an independent Zoom endpoint have been verified through Yap. The broader real-call acceptance matrix below remains in progress.

## Native bridge implementation contract

`ZoomMeetingDriver` implements the `MeetingDriver` boundary through the `YapZoomBridge` target. The app should construct it using `makeZoomMeetingDriver(accountClient:)` with the same actor used by its Zoom connection settings. `MeetingCoordinator` owns visible state and rejects callbacks tagged with a different session UUID. The driver must own the SDK instance, callback delegates, native video elements, and subscriptions for the whole session. SDK callbacks must be marshaled onto the main actor before calling `onEvent`; copy scalar values before crossing threads rather than sending SDK objects between actors.

The native boundary is Objective-C and compiles against the downloaded public SDK headers. The Swift wrapper receives copied, in-memory event payloads on the main actor. SDK logging remains disabled. Native consent sheets retain the SDK-provided text and require a person to act; Yap does not silently accept recording or archiving notices.

| Yap operation | Current documented SDK surface | Implementation requirement |
|---|---|---|
| Initialize custom UI | `ZoomSDKInitParams.needCustomizedUI`, `[ZoomSDK sharedSDK]`, `initSDKWithParams:` | Choose custom UI before initialization. Keep SDK logging off in normal use. |
| Authenticate application | `ZoomSDKAuthContext.jwtToken`, `ZoomSDKAuthService.sdkAuth`, auth delegate | Wait for successful asynchronous authentication; obtain a fresh signed identity for each meeting attempt. Fail closed if muted-on-entry cannot be confirmed after authentication. |
| Join | `ZoomSDKMeetingService.joinMeeting:` with `ZoomSDKJoinMeetingElements` | Pass the meeting ID/passcode or supported vanity ID and user ZAK; preserve the source link only in memory. |
| Host | `startMeetingWithZAK:` with `ZoomSDKStartMeetingUseZakElements` | Use `SDKUserType_APIUser`, the real meeting ID returned by the own-user REST creation, and valid host ZAK. |
| Leave/end | `leaveMeetingWithCmd:` | Distinguish local leave from ending for everyone; wait for actual terminal state. |
| Native participant tiles | `getVideoContainer`, `createNormalVideoElement:`, element `userid`, `getVideoView`, `resize:`, `setResolution:` | Bind only when the camera-on view is attached with usable bounds. Setting `userid` starts the initial subscription; do not immediately duplicate it with `subscribeVideo:YES`. Coalesce rate-limit failures into bounded delayed retries. Release off-page subscriptions and clean elements on leave. |
| Camera quality and tile labels | `enableCatchHDVideo:`, `isCatchHDVideoOn`, `getVideoStatisticInfo`, `displayUserNameOnVideo:` | Request HD on explicit camera activation; distinguish the preference from observed send resolution/FPS. Hide Zoom's native name/mute decoration before renderer creation because Yap supplies its own accessible overlay. |
| In-meeting chat | `getMeetingChatController`, `sendChatMsgTo:`, chat delegate | Use supported recipient/message objects; represent host-disabled chat correctly. |
| Sharing | `getASController`, `startAppShare:`, `startMonitorShare:`, `stopShare`, `getShareContainer` | Install the AS controller delegate on confirmed InMeeting: the controller does not exist before admission. Enumerate ScreenCaptureKit windows only after explicit Share action; validate each window with `isShareAppValid:` and transmit using the SDK. Received content uses native SDK share elements. Only show “Sharing” following SDK confirmation. |

Sources: [initialization options](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_init_params.html), [SDK authentication](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_auth_service.html), [meeting service](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_meeting_service.html), [join parameters](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_join_meeting_elements.html), [host parameters](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_start_meeting_use_zak_elements.html), [video container](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_video_container.html), [native video view](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_video_element.html), [chat controller](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_meeting_chat_controller.html), [share controller](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_a_s_controller.html).

The coordinator's current `host(displayName:title:)` creates and starts an instant meeting. A zero-ID SDK start originally failed with WrongUsage; the intermediate Unknown-user-type attempt stalled. Both paths have been replaced by the verified APIUser/REST-returned-ID flow. Starting an existing scheduled meeting as host still needs a host-request overload carrying its meeting number before that workflow is connected. The UI's host flag is never a substitute for Zoom identity.

The native driver bounds connection attempts with a 60-second watchdog and reports an unconfirmed leave after 15 seconds without pretending the SDK has stopped. Safe connection-state diagnostics use `app.yap.zoom`; they exclude credentials, identities, meeting numbers, and invitations. Proprietary SDK logging remains disabled.

Video diagnostics record SDK result/data-type codes, native view bounds/attachment, and aggregate video dimensions/frame rates. A brief initial blank view while the first network frames arrive is expected; it is not itself a subscription failure. The September 6 live run had a usable remote renderer at 10:13:30 and incoming frame statistics by 10:13:33. See [Video-Runtime.md](Video-Runtime.md).

The later canonical build received 1280 × 720 video in an enlarged gallery, then exposed a focus-layout handoff regression. The corrected wrapper claims ownership after actual attachment/nonzero layout, honors the parent sizing proposal, and prevents outgoing mounts from reclaiming a replacement renderer. Ten focused cases and the actual production-wrapper window fixture pass. The final SDK release is installed, but its live retry stopped at an app-owned credential prompt before SDK initialization. Separate earlier intervals verified received and transmitted 720p; the final focus correction still needs a real SDK retry. [Focus regression and correction](Video-Runtime.md#focus-handoff-regression-and-bounded-correction).

## Build and signing

The package must build with Xcode 26 or later for SDK 7+. Link `ZoomSDK.framework` and the exact companion libraries declared in the downloaded package. A local SwiftPM target can receive the framework search path and `.linkedFramework("ZoomSDK")`; the app packaging step must additionally copy and sign the framework, dylibs, and SDK helper/plugin bundles in the locations required by the sample. Do not assume copying a single framework is sufficient. Confirm all binaries support arm64 and the app loads from a clean launch outside the development directory. Zoom's current macOS SDK does not support App Sandbox, so Yap targets Developer ID signing and notarization. [SDK setup and packaging](https://developers.zoom.us/docs/meeting-sdk/macos/get-started/).

The package manifest detects this installed SDK path, or `YAP_ZOOM_SDK_PATH`. When unavailable, the factory returns the explicit unavailable driver. `Scripts/embed-zoom-sdk.sh` copies the complete runtime and bundled audio plug-in without installing a system driver, verifies dependency paths, and re-signs staged native code. The app now uses **Developer ID Application: Michael Grinich (VSVHNQP588)** and bundle identifier **`com.grinich.yap`**. The earlier media proofs used the local Whoosh Personal Development certificate under `app.whoosh.personal`. Signing is selected through gitignored `signing.local.json`; `YAP_SIGNING_IDENTITY` is an explicit override. The script preserves the original downloaded SDK, and the executable resolves its runtime from its own Frameworks directory. Public distribution additionally needs the applicable notarization workflow. [Personal signing and verification](Personal-Signing.md).

The final renderer release passed native icon compilation, three App Intents/three App Shortcuts, and all 97 embedded Mach-O/signature/runpath checks. Its compilation took 8.54 seconds: [`work/renderer-attachment-final-release.log`](../../work/renderer-attachment-final-release.log). Installation is recorded in [`work/renderer-attachment-final-install.log`](../../work/renderer-attachment-final-install.log). The full pure suite at that checkpoint passed **198 tests in 29 suites**, with zero failures or skips: [`work/full-pure-tests-zero-size-handoff-final.log`](../../work/full-pure-tests-zero-size-handoff-final.log). These fixture tests are separate from live SDK media tests.

**Historical self-signed checkpoint:** Changed signed builds retained the same certificate-bound designated requirement. Google/Zoom account loading, SDK initialization, authentication, and hosting continued through later updates and relaunches without another blocking Keychain prompt. After the authorized System Settings removal and re-addition of the exact Yap app, Screen Recording access also survived subsequent signed updates. A selected synthetic test window was captured and visibly delivered to a separate native Zoom endpoint. Installing the AS delegate after admission restored the Stop control; switching A → B delivered B remotely, and Stop cleared sharing at both endpoints. The earlier stale-A label is historical: the canonical installed app now shows B in its banner while B is delivered remotely, with a safe `source-confirmed` diagnostic. Stop clears both clients. Native Zoom also shared animated B into Yap; real changing pixels were observed, and stopping in native Zoom cleared Yap's incoming panel. Incoming camera video is visible in the gallery; the focus-layout handoff correction has seven passing fixtures and awaits its live retry. Gallery camera off/on and chat/window resize worked before that defect. Separate intervals now establish received 1280 × 720 at approximately 22–30 fps and sent 1280 × 720 at 24–30 fps with actual native Zoom receiver video; these are not simultaneous two-way HD or 1080p. Later outbound measurements returned to 640 × 360. See [window-sharing evidence and its limits](Video-Runtime.md#later-outgoing-window-sharing-checkpoint).

## Remaining permission review

**Historical checkpoint:** the observations below concern the earlier bundle migration and local permission state. They do not describe the current review candidate's authorization or acceptance status.

The most recent pre-migration attempt stopped before SDK initialization at the app-owned `app.whoosh.credentials` Keychain read. Google bootstrap was waiting on that read, and Zoom was waiting for the shared vault lock. The SDK-owned safe meeting storage was not the blocker in that attempt. Earlier prompt-free account/SDK accesses below describe specific successful runs and do not establish a permanent resolution. The narrow inspection did not prove the cause of the current prompt.

**Historical Developer ID migration:** the **`com.grinich.woosh`** app, signed with **Developer ID Application: Michael Grinich (VSVHNQP588)**, was built and installed at `/Users/mg/Applications/Whoosh.app`; its identifier and Team ID are verified. Existing credential service/account keys are retained rather than renamed, and five allowlisted preferences are copied once to the new defaults domain before model creation. Existing new-domain values are never overwritten. Five migration tests pass; the normal-context full suite passed **203 tests in 30 suites**, zero failures/skips. [Release](../../work/developer-id-bundle-migration-release.log), [installation](../../work/developer-id-bundle-migration-install.log), [signature](../../work/developer-id-installed-signature.txt), [full suite](../../work/full-pure-tests-bundle-migration-root.log).

The old app's agenda refreshed around 12:47 after the earlier prompt. The new Developer ID app presented its own app-vault authorization prompt, so account access and the corrected live SDK focus test remained pending protected macOS authorization at this checkpoint. Earlier self-signed live results remain historical evidence. At that historical checkpoint, both Zoom and Google used their native loopback OAuth clients and redirects without sending the bundle ID. The upcoming managed Zoom flow changes its own callback only; Google authentication is unchanged. SDK-owned storage, notification/launch-at-login registrations, and macOS permission decisions are not migrated by copying defaults.

Accessibility remains off. Yap does not offer remote control, and no additional Accessibility or Input Monitoring grant was made to suppress a request. Zoom's current permissions guide associates Accessibility with remote control and Screen Recording with screen sharing. The SDK's public initialization, sharing, and settings headers expose no permission-request suppression option. `hideRemoteControlItemOnMoreMenu` hides UI only; the remote-control auto-accept API itself requires Accessibility and is not a suppression mechanism. An older forum reference to `enableGlobalShortcuts` is absent from the current 7.1.5 public headers and current configuration reference, so it is not used. [Zoom permissions](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0064868), [current meeting configuration](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_meeting_configuration.html).

**Historical self-signed checkpoint:** The earlier diagnostic log recorded stale code-requirement matches for Documents access and Accessibility, including a request from the SDK's CptHost helper. It did not establish the accessed Documents path or a separate Input Monitoring request. Canonical installation has now completed at `/Users/mg/Applications/Whoosh.app` using `Scripts/install-personal-app.sh`; the actual process path and unchanged signing requirement were verified. [Install log](../../work/canonical-personal-install.log). Both connected accounts, own-meeting hosting, and previews worked without a new blocking Keychain or recording prompt. The fresh [canonical permission observation](../../work/whoosh-canonical-permission-observation.log) contained no Documents request/mismatch during launch and hosting, with only the existing Accessibility mismatch. That toggle remained off and untouched. This bounded result does not prove that all future permission requests are eliminated.

The supported `ZoomSDKRecordSetting.setRecordingPath:` can redirect recording output to an existing directory. It is not an initialization option, and there is no evidence yet that the observed Documents request comes from recording rather than loading app resources from Documents. Changing that path has therefore not been applied as a speculative permission fix. [Recording path API](https://marketplacefront.zoom.us/sdk/meeting/macos/interface_zoom_s_d_k_record_setting.html). No SDK binaries, macOS permission databases, existing grants, or trust settings were altered during this review.

## Required real-call tests

| Test | Acceptance evidence |
|---|---|
| Own-account join | Two or more independently connected endpoints see and hear the correct identity. |
| Host instant meeting | Create/start, obtain a genuine invite, participant joins, host admits when waiting room is enabled. |
| Native tiles | Two independent cameras visibly move in Yap; no avatar counts recorded as video counts. |
| Microphone/camera | Another endpoint observes mute/unmute and camera on/off; failed permission leaves the control truthful. |
| Chat | A message sent in Yap appears remotely and a remote message appears locally. |
| Window share | Another endpoint sees exactly the selected window; changing window, closing source, and stopping are observed remotely. |
| Leave/end | Local leave does not end a guest's call; host end closes the meeting for all. |
| Recovery | Network loss, reconnect, sleep/wake, device unplug, and stale SDK callbacks do not accidentally enable media. |
| Repetition | Twenty join/leave cycles have no retained video views, callbacks from old meetings, or continuing media sessions. |
| External meeting | Run only after the app and human identity have the needed authorization. |

For capacity, advance through 49, 64, 100, and 144 *independent moving video feeds*. Record SDK version, Mac hardware, display resolution, subscribed and actually rendered feeds, delivered resolution/frame rate, UI frame times, memory, CPU, network bandwidth, and remote sharing behavior. Stop on SDK rejections or resource pressure and present the measured working limit with pagination. The current demo validates grid and session behavior only.
