# Yap: Zoom review submission package

Updated September 7, 2026 for **Yap 0.1.1, build 2, signed and notarized GitHub draft**, source `283e37cfddc47cac213be884f5dab6582a8713b0`. Main CI and the actual-SDK release workflow passed. Independent downloaded-artifact verification passed; native installation acceptance remains pending. The immutable **0.1.0, build 1** tag/draft is superseded and must not be installed or promoted. Its historical CI, scan and notarization evidence below belongs to merged main commit `ac394cd9a0834da2d3b58faed5e52efee980d246` ([PR #4](https://github.com/grinich/yap/pull/4)). This is a working submission package; the app has not been submitted or approved. Fields marked **pending** must be completed from the deployed release before submitting an attestation. Do not put credentials, private test meetings or confidential Zoom review results in this public file.

**Portal checkpoint reported by the release operator:** production General app `z9T2ySUsSySXlDZgwzNBew` exists in **Draft** with the saved app header **Yap**, verified by the release operator in Chrome and a screenshot on September 7, 2026. The Publish page still says **Not ready**, identifying **App Icon, App Gallery and Application Overview** as missing upload requirements. Production native PKCE is enabled; the public client ID is `_Xz_EnBNS3OPtUmqZ1og3A`, and the SDK client ID is `UHoml3aIQpy86gZeijjfpQ`. These are public identifiers, not secrets. The production contact is **Michael Grinich**, **mgrinich@gmail.com**, role **Developer**, confirmed visually in the portal; the public-client OAuth option remains enabled. The PKCE explanation, loopback redirect, all three production scopes and scope justification are entered. The production technical stack, retention answer and GitHub documentation links have been saved and confirmed. The user selected an initial non-EU audience; **EU availability is saved as false**. The production service is deployed at `https://meeting-auth.mgrinich.workers.dev`; HTTPS health and anonymous-signature rejection are verified, and live production browser PKCE plus authenticated service checks now pass. Native GUI/Keychain integration, SDK initialization, refresh/revocation and full native acceptance remain pending. The saved portal name is confirmed; the final listing icon upload is still blocked. Draft status is not submission or approval.

## Submission route and remaining decisions

- **Audience:** external users outside the EU initially, as authorized by the release owner; production EU availability is saved as false. This is an audience setting, not a data-residency promise. User-managed General app with the macOS Meeting SDK enabled. A reviewed unlisted listing is a suitable initial distribution route; unlisted apps still undergo review. Internal sharing alone covers users within the developer's Zoom account, not everyone at the same company. [Submission audience](https://developers.zoom.us/docs/distribute/app-submission/submit-apps-review/), [review process](https://developers.zoom.us/docs/distribute/app-review-process/)
- **Naming:** the selected product name is **Yap**, with the tagline **“For people who professionally yap for a living.”** The former Zoom-variant branding has been retired. Update the portal draft, icon, consent screen and binary consistently; name selection is not trademark clearance or Zoom approval. [Naming requirements](https://developers.zoom.us/docs/build-flow/app-listing/app-icon-and-app-name/)
- **Licensing:** the source publication is authorized separately from distribution of the integrated SDK application. Zoom's API terms require Marketplace publication or advance written approval for third-party app use; proprietary SDK files must not be redistributed as standalone public dependencies. Do not describe this as an official Zoom product or a way to bypass Zoom licenses. [API terms, sections 2, 6 and 7](https://www.zoom.com/en/trust/legal/zoom-api-license-and-tou/)
- **Beta is not a publicity workaround:** approved external authorization-URL sharing has time limits and conditions against publicizing the app. The existing public repository must be disclosed if requesting that route. Prefer the ordinary external review path unless Zoom approves an exception. [Beta sharing conditions](https://developers.zoom.us/docs/distribute/sharing-private-and-beta-apps/)
- **Release readiness:** Yap **0.1.1, build 2** is signed and notarized in an unpublished GitHub draft. [Main CI](https://github.com/grinich/yap/actions/runs/34169878107) and [release run 34169899197](https://github.com/grinich/yap/actions/runs/34169899197) passed at `283e37cfddc47cac213be884f5dab6582a8713b0`; the release completed actual-SDK tests, signing, app/DMG notarization, stapling and Gatekeeper checks. Independent downloaded hashes, app/DMG signatures, stapled notarization, Gatekeeper, DMG integrity and ZIP update-signature verification passed without launching or installing the app. [Current artifact evidence](Releases.md#current-candidate-evidence). The superseded **0.1.0** draft remains unchanged only as historical evidence, not for installation or promotion. The managed token proxy/signer is deployed at the configured workers.dev origin; public review documents use GitHub. Production browser PKCE, token exchange, a bounded recordings-list request and an SDK-signature response passed in memory. Native GUI/Keychain integration, SDK initialization, refresh/revocation, fresh-Mac acceptance and final review uploads remain pending. Apple notarization is separate from Zoom approval.

## Listing fields and copy

| Field | Proposed value or required completion |
| --- | --- |
| App name | Yap. The saved portal header now matches the binary/product; verified September 7, 2026. Maximum 50 characters. |
| App type | General app; user-managed OAuth; native macOS Meeting SDK; public-client PKCE. |
| Audience / discoverability | External, initially outside the EU; production EU availability is saved as false. Unlisted discoverability remains a proposed review route unless separately confirmed. |
| Platform | Apple silicon Mac, macOS 26 or later. |
| Category | Productivity / meetings, selecting the closest categories offered in the portal. |
| Developer/company | Michael Grinich, individual developer; no separate company is asserted. |
| Contact | Michael Grinich, `mgrinich@gmail.com`, role Developer; verified in the production portal. Public support uses GitHub Issues. Use the email for private privacy/security matters, never public Issues for secrets. |
| Home / documentation / support | Verified HTTP 200: [Home](https://github.com/grinich/yap), [Guide](https://github.com/grinich/yap/blob/main/Services/zoom-auth/site/guide.md), [Support](https://github.com/grinich/yap/issues). Source: `https://github.com/grinich/yap`; public bug tracker: `https://github.com/grinich/yap/issues`. |
| Privacy / Terms | [Privacy](https://github.com/grinich/yap/blob/main/PRIVACY.md) and [Terms](https://github.com/grinich/yap/blob/main/TERMS.md) both return HTTP 200 and cover personal/managed modes. The free-preview Terms are published in the GitHub repository at the operator’s request, effective September 7, 2026. Source: [Privacy](../PRIVACY.md), [Terms](../TERMS.md). |
| Download | Candidate **0.1.1, build 2** is signed/notarized and held in an unpublished GitHub draft with `Yap.dmg`, its `.sha256` file and the other release assets. Intended post-publication URL: `https://github.com/grinich/yap/releases/download/v0.1.1/Yap.dmg`. Independent downloaded-artifact verification passed; there is no current public reviewer download. Arrange an approved private delivery path while distribution approval remains pending. The superseded 0.1.0 draft must not be installed or promoted. |
| Icon / gallery | Final icon, architecture PDF and security reports are prepared locally but not uploaded. A production-view fixture with four fictional participants is ready; the actual native gallery screenshot awaits an unlocked Mac. The portal requires 1200 × 780-pixel App Gallery images; the Cover is 1824 × 176, and the icon is at least 160 pixels and under 1 MB. Prepared files and file URLs are not completed portal uploads. |

The production build flow requires app information, contacts and technical design. Its submission page checks required URLs and domain ownership. Use a domain the operator can verify, or the portal's manual verification process; a public GitHub page does not establish ownership of `github.com`. [Production fields](https://developers.zoom.us/docs/build-flow/prep-app-for-prod/), [submission and domain validation](https://developers.zoom.us/docs/distribute/app-submission/submit-apps-review/)

**Short description — proposed:**

For people who professionally yap for a living. Join Zoom meetings, follow your agenda, and revisit recordings with saved chat and transcripts.

**Long description — proposed:**

Yap is an independent macOS application for people who use Zoom for meetings. Sign in to your own Zoom account to join meetings, start an instant meeting, and use microphone, camera, screen sharing and meeting chat through Zoom's Meeting SDK. Meeting access and recording controls respect your Zoom account and host permissions.

Open Recordings to browse your own Zoom cloud recordings, search by title or date, and load earlier months. Play a recording in the main view or a dedicated window, change available video layouts without losing your place, adjust speed, and follow saved chat or a spoken transcript when those files are available. Save a video or export a transcript to a location you choose.

Google Calendar is optional and read-only. It shows upcoming Zoom meetings from calendars you select. A Zoom account is required; cloud-recording features require the corresponding Zoom plan, permissions and completed cloud files. Yap is independently developed and is not affiliated with or endorsed by Zoom.

Update the product name in this copy if naming is resolved by a rename. Do not add a “no backend,” end-to-end encryption, certification, uptime or free-service claim without evidence.

## Exact scopes and functional justification

Current client source: [ZoomCredentials](../Sources/YapMeetings/ZoomCredentials.swift), [ZoomAccountClient](../Sources/YapMeetings/ZoomAccountClient.swift). Select these **user-level granular scopes**, not their `:admin` or `:master` alternatives. The [signer](../Services/zoom-auth/src/index.ts) requires the same three scopes and adds none.

| Scope | Endpoint / data | Copyable justification |
| --- | --- | --- |
| `user:read:zak` | `GET https://api.zoom.us/v2/users/me/zak`; the signed-in user's five-minute ZAK. | Obtain the consenting user's ZAK immediately before starting or joining a meeting, so the native Meeting SDK acts as that user. No other account member's ZAK is requested. |
| `meeting:write:meeting` | `POST https://api.zoom.us/v2/users/me/meetings`; instant meeting ID and creation result. | Create an instant meeting only when the user chooses to host. The SDK then starts that meeting with the user's ZAK. The app does not create meetings for other users or send invitations through this API. |
| `cloud_recording:read:list_user_recordings` | `GET https://api.zoom.us/v2/users/me/recordings`; paginated meeting/file metadata and returned download URLs. | List the consenting user's cloud recordings so they can search, play and save completed videos and read available saved chat/transcript files. Authenticated file downloads use the returned URLs; the app does not delete or change cloud recordings. |

Zoom documents these endpoints and granular scopes in its [Users API](https://developers.zoom.us/docs/api/users/) and [Meetings API](https://developers.zoom.us/docs/api/meetings/). The app does not call the separate meeting-transcript REST endpoint, so do not request `cloud_recording:read:meeting_transcript` merely because the player displays a downloaded VTT file. Cloud recording controls during a live meeting use SDK permission APIs rather than a recording-write REST scope.

## Technical design and data paths

**Client architecture — verified in source:** Swift/SwiftUI and AppKit interface; Objective-C bridge to Zoom Meeting SDK 7.1.5 (84750); Foundation URLSession for REST/downloads; AVPlayer with a resource loader for authenticated playback; CryptoKit for PKCE; macOS Keychain for app credentials; Sparkle for signed updates. No RTMS, meeting bot, AI model or advertising integration is implemented.

1. The Mac opens Zoom authorization in the user's system browser. It generates random state and an S256 PKCE verifier/challenge, then receives the code on a temporary listener bound only to `127.0.0.1` and an ephemeral port. It validates state, path, Host and a single authorization code, then closes the listener. **Personal mode** exchanges/refreshes directly with `https://zoom.us/oauth/token`. **Managed mode** sends the code, verifier and redirect, or a refresh token, to the configured HTTPS service at `/v1/oauth/token`; the service pins the production public client ID and forwards only accepted fields to Zoom.
2. The Mac calls Zoom's REST APIs with the user's access token. REST transport uses ephemeral sessions without URL/cookie storage and refuses redirects. Refresh operations are coalesced; account generation checks prevent a late response from restoring disconnected credentials.
3. In managed mode, the Cloudflare Worker returns the Zoom access/refresh tokens and an HMAC-signed authorization grant bound to the access token's SHA-256 hash and the configured public client ID. The Mac saves these in Keychain. To get an SDK JWT, it sends the access token and grant to `/v1/meeting-sdk/signature`. The Worker verifies the grant and makes a fresh `GET /v2/users/me/zak` request to Zoom before signing a native SDK JWT with its server-held secret. Personal mode instead signs locally using the owner's imported developer configuration. ZAK and the SDK JWT have separate functions. [SDK authorization](https://developers.zoom.us/docs/meeting-sdk/auth/)
4. The native SDK exchanges live meeting audio, video, shared screens, participant metadata and in-meeting chat with Zoom. The project-operated signer must not be described as carrying this media. The SDK's recording permission/consent handling and Apps and privacy panel remain available through the bridge.
5. Recordings are listed by monthly date windows and paginated. Playback/text requests begin at validated HTTPS Zoom media URLs. HTTPS CDN redirects drop the Zoom OAuth Authorization header outside trusted Zoom hosts. Video uses bounded byte ranges with an 8 MiB per-loader memory cache; fallback playback can use a temporary file. Chat/transcript inputs are limited to 8 MiB each, validated for content type/encoding and rejected if they are HTML/auth pages.
6. Optional Google Calendar OAuth and reads run directly between the Mac and Google. User-initiated links and bug reports open in the browser. Configured updates contact the release host through Sparkle; system-profile sending is disabled.

**Production PKCE explanation — copyable:**

This is a native macOS application using Zoom's public-client OAuth PKCE flow. It cannot safely hold a confidential OAuth client secret. Register `http://127.0.0.1/callback` in the public client's redirect allow list; the application supplies a fresh ephemeral port for each authorization. Only the port varies. Browser consent, S256, cryptographically random state, a loopback-only listener and exact callback validation protect the flow. A same-origin HTTPS token proxy forwards the PKCE exchange/refresh using the fixed public client ID and issues token-bound signing authorization; it does not use confidential-client OAuth credentials. The separately held SDK secret signs SDK JWTs only after that authorization and a live ZAK check.

Zoom explicitly documents this loopback flow for eligible native/PKCE clients and may request the production public-client justification above. Use the **Production public client ID** for a first-time review build. [OAuth PKCE and loopback](https://developers.zoom.us/docs/integrations/oauth/#using-a-loopback-redirect-uri), [first-time submission](https://developers.zoom.us/docs/distribute/app-submission/submit-apps-review/)

| Signer field | Implemented contract / remaining deployment evidence |
| --- | --- |
| HTTPS URL and deployment version | Production origin: `https://meeting-auth.mgrinich.workers.dev`; API-only deployment `23757fca-b323-4f65-a8ce-42255449c072`. Routes are `POST /v1/oauth/token`, `POST /v1/meeting-sdk/signature`, and `GET /health`; documents are hosted on GitHub. Health returns 200 and `/terms` returns 404. Authentication logic from service fix [`9d4e1dd`](https://github.com/grinich/yap/commit/9d4e1dd) is unchanged from the successful scoped live checks on preceding deployment `31a76568-0191-4f73-a3e7-d530fbc52966`. The source bundle fixes this origin; it is not supplied by meeting links. Native app acceptance remains pending. |
| Caller authentication and authorization | Token proxy pins `ZOOM_PUBLIC_CLIENT_ID`. Signing requires `Authorization: Bearer` plus `X-Signing-Authorization`; the HMAC grant's audience, exact token hash and expiry are checked, then Zoom validates current access through `/users/me/zak`. No anonymous signature endpoint or profile-read scope. |
| SDK secret / JWT lifetime | `ZOOM_SDK_CLIENT_SECRET` and independent `SIGNING_GRANT_SECRET` are server secret bindings. SDK JWT uses `iat = now - 30`, `exp = tokenExp = iat + 3600`. Signing grants last at most one hour and no longer than the access-token lifetime. The release operator reports both secrets provisioned through Cloudflare secret bindings, with an independently generated grant secret and temporary plaintext removed. A secret-rotation exercise is not claimed. |
| Data received and outbound calls | Proxy transiently handles the code, PKCE verifier, loopback URI, refresh/access tokens, scope and expiry. Signature route transiently handles access token, signing grant and upstream ZAK; request body must be empty JSON. Fixed authentication upstreams are Zoom's OAuth token and current-user ZAK endpoints. No meeting numbers, recording content, calendar data, profiles or email fields are requested. |
| Storage and retention | No token/user database, KV store, authentication response cache or refresh-token background job. There is no static asset binding; public documentation and Terms are hosted on GitHub. Tokens live in request memory and return to the client. Rate-limit bindings receive hashes of the source IP and access token. Explicit application logging emits only the constant `auth_service_failure` event; invocation logs and traces are disabled in Wrangler. Cloudflare can retain platform metadata; do not claim infrastructure-wide zero retention. Verify deployed settings. |
| Abuse protection | Configured bindings: 120 requests per 60 seconds per source-IP hash, plus 10 signature requests per 60 seconds per token hash; these are Cloudflare binding limits, not a claimed global hard cap. Input body ≤16 KiB, upstream body ≤64 KiB, 15-second upstream timeout, no upstream redirects, no credential query strings, no browser Origin on authentication routes, no unknown exchange fields. Errors use fixed codes; API responses are `no-store`. Public documentation is served by GitHub, separately from this API. |
| Operator and subprocessors | Michael Grinich operates the service on Cloudflare Workers at `meeting-auth.mgrinich.workers.dev`; the release operator verified the Free plan. No region-specific residency promise is made. Contact: `mgrinich@gmail.com`. |
| Deauthorization/deletion | No stored per-user token record to delete. Each new SDK JWT requires Zoom to accept the current token; revocation prevents new issuance when Zoom rejects access. Already-issued SDK JWTs can remain valid until expiry. No deauthorization webhook is implemented. Local disconnect removes the grant/tokens on that Mac; security counters/platform records follow provider retention. |

Source evidence: [Worker implementation](../Services/zoom-auth/src/index.ts), [Wrangler configuration](../Services/zoom-auth/wrangler.jsonc), [managed client configuration](../Sources/YapMeetings/ZoomPublicConfiguration.swift), [remote signer](../Sources/YapMeetings/ZoomRemoteSDKSigner.swift), and [synthetic service tests](../Services/zoom-auth/test/auth.test.ts). The release operator verified the Free plan. Cloudflare documents three-day Workers Logs retention on Free; this does not specify retention for all infrastructure data. [Workers Logs](https://developers.cloudflare.com/workers/observability/logs/workers-logs/), [Cloudflare privacy policy](https://www.cloudflare.com/privacypolicy/)

**Storage/deletion — current client:**

| Data | Current handling / limitation |
| --- | --- |
| OAuth tokens, signing grant and account configuration | Consolidated non-synchronizing Keychain vault and process cache. Local disconnect removes Zoom tokens/grant and invalidates recording state; personal developer configuration can remain. Disconnect is not server-side grant revocation. |
| Recording metadata, chat and transcript | In-memory player/library state; cleared on account changes/teardown. Hiding a view may retain its paused player and cache. |
| Video cache / download-to-play | Bounded in-memory ranges or a temporary local MP4 retained while its playback source is owned, then removed on normal cleanup. Do not promise secure erasure or cleanup after every process crash. |
| Explicit video saves / transcript exports | User-selected local files remain until the user removes them. They are not independently encrypted by the app. |
| Google agenda | Local account-bound cache with user-only permissions; cleared on disconnect. Includes event metadata and possibly meeting passcodes. File permissions are not a claim of application-level encryption. |
| SDK-owned data | Zoom manages its own local storage, avatar originals and safe-meeting Keychain item. The app does not inspect or promise deletion of those internals. |
| Logs | App lifecycle diagnostics exist; SDK logging is disabled. No automatic bug-report attachments or app marketing analytics are implemented. The Worker emits fixed error-event text and disables invocation logs/traces; deployed host metadata/retention remains to be verified. |

Sources for these implementation statements: [credentials](Credential-Storage.md), [calendar](GoogleCalendar.md), [recordings](Recordings.md), [media loader](../Sources/YapAppUI/RecordingMediaLoader.swift), [text loader](../Sources/YapAppUI/RecordingChatLoader.swift), [Zoom bridge](../Sources/YapZoomBridge/YapZoomBridge.m). [Privacy](../PRIVACY.md) now describes both connection modes; confirm it matches the deployed configuration before publication.

## Reviewer installation, use and removal

Supply the exact release URL/version, production authorization configuration, and any needed synthetic test account through the portal's private test-credentials field. Do not provide the developer SDK secret or personal account. A reviewer with only a Basic account may not have cloud-recording access; arrange a licensed synthetic test host with completed MP4, CHAT and VTT artifacts, plus a separate participant. State any external-meeting authorization prerequisite up front.

1. On an Apple silicon Mac with macOS 26+, download the signed/notarized DMG, copy the app to Applications and open it. Record the Gatekeeper result. The review build must already know the public OAuth/signing configuration; asking a reviewer to create a developer app is not release onboarding.
2. Open Settings and choose **Sign in with Zoom**. Complete browser consent with the production public client. Confirm the connected state. Repeat once with Cancel/denied consent; the app must remain disconnected without a false success.
3. Choose Host and confirm one instant meeting is created. Its requested defaults mute entry, disable camera, enable waiting room and disable automatic cloud recording/AI summaries. Verify the actual account-enforced behavior using the second endpoint.
4. Admit/join the second endpoint. Exchange synthetic microphone/camera content, one meeting-chat message and a selected test-window share. Confirm privacy controls, Apps and privacy access, Stop sharing, Leave and host End behavior. Do not use private meeting data.
5. Join a separate authorized external-host meeting after Zoom grants the needed access. Verify the human user's identity is attributed through ZAK. Do not label this passed based only on an internal meeting.
6. If the test host has cloud-recording permission, exercise Start, Pause, Resume and Stop with participant notice. Confirm that server/SDK permission errors remain visible. Processing is asynchronous; pre-existing completed recordings keep the remaining tests independent of processing time.
7. Open **Recordings**. Find a prepared recording, search by title/date, load an earlier month, play/seek, change speed and an available video layout, and double-click a row to open its own player. Reopen the library during playback and confirm refresh does not restart it.
8. Open available saved chat and spoken transcript. Confirm timing follows playback; click a transcript cue to seek, then export a VTT and save a video to an explicitly chosen folder. A recording without those files must not show invented content.
9. After leaving calls, use Settings → **Disconnect Zoom**. Confirm playback/windows and account-owned lists/text clear; reconnect to demonstrate recovery. Separately remove the app from Zoom Marketplace's added-app management and confirm a revoked grant cannot fetch fresh data. Reconnect only through explicit consent. Local disconnect does not itself revoke Zoom authorization.
10. To uninstall, disconnect connected accounts, quit and delete the app. Explicit exports remain user-owned; remove them separately if desired. Zoom's grant can be removed independently. The app does not claim to erase SDK-owned storage or all preferences by deleting the application bundle. Confirm the deployed signer's stateless storage and revocation behavior against the contract above.

Optional Google Calendar can be left disconnected for the Zoom review. A separate calendar test uses a synthetic consented account and verifies read-only selected calendars and disconnect; it is not required to host a Zoom meeting.

## Security evidence and truthful TDD answers

**Current 0.1.1:** [main CI 34169878107](https://github.com/grinich/yap/actions/runs/34169878107) and [actual-SDK release 34169899197](https://github.com/grinich/yap/actions/runs/34169899197) passed at `283e37cfddc47cac213be884f5dab6582a8713b0`. The release completed signing, app/DMG notarization, stapling and Gatekeeper checks and uploaded five artifacts to the draft. Independent downloaded-artifact verification passed at 23:44:30 UTC on September 7, 2026; current Swift CodeQL is still pending at this checkpoint. [Current evidence](Releases.md#current-candidate-evidence).

The historical 0.1.0 release baseline is **644 Swift tests in 77 suites, 39 Python release/script tests and 23 authorization-service tests** at `3c316ce`, with [CI 34166643648](https://github.com/grinich/yap/actions/runs/34166643648) and [CI 34166640977](https://github.com/grinich/yap/actions/runs/34166640977) passed. [Merged-main CI](https://github.com/grinich/yap/actions/runs/34166962118) passed, and the [release run](https://github.com/grinich/yap/actions/runs/34166976838) passed actual-SDK tests and app/DMG notarization, stapling and Gatekeeper checks. [Merged-main CodeQL run 34166962123](https://github.com/grinich/yap/actions/runs/34166962123) passed on `ac394cd9a0834da2d3b58faed5e52efee980d246`. CodeQL **2.26.4** completed Swift analysis with **28 rules / 0 results** and JavaScript/TypeScript with **103 rules / 0 results**, without reported analysis errors or warnings. The repository query returned **zero open code-scanning alerts** at this checkpoint. Current service validation is separately **24 tests** plus the actual local `workerd` regression check for fix `9d4e1dd`. The [evidence inventory](SecurityEvidence.md) and [architecture PDF](../output/pdf/Zoom-Architecture.pdf) are prepared but not uploaded. These results are engineering evidence, not an independent penetration test, certification or proof of every control. [Security requirements](https://developers.zoom.us/docs/distribute/security-requirements/)

The managed service at `https://meeting-auth.mgrinich.workers.dev` now runs API-only Cloudflare deployment `23757fca-b323-4f65-a8ce-42255449c072`; health returns 200 and `/terms` returns 404. Public documentation and Terms use GitHub. The same authentication logic, including service fix [`9d4e1dd`](https://github.com/grinich/yap/commit/9d4e1dd), passed a live production browser PKCE check at this host on preceding deployment `31a76568-0191-4f73-a3e7-d530fbc52966`. The check passed consent and the loopback callback, token exchange with all three required scopes, one bounded recordings-list page, and an SDK-signature response. Credentials stayed in the test process memory; this check did not write Keychain credentials, initialize the native SDK, exercise the app GUI, or test token refresh or revocation.

The earlier OAuth error **4700** was resolved by regenerating the production Beta authorization URL. A subsequent service failure was reproduced in the actual local Workers runtime: `redirect: "error"` was unsupported. The fix uses `redirect: "manual"` and explicitly rejects upstream 3xx responses. **24 service tests** and an actual local `workerd` before/after regression check passed. This current service validation is separate from the release baseline of 23 service tests. Full native app acceptance remains pending.

**Saved Technical Design progress:** Security **3/3**; Overview **2/5**. The technical stack is saved/read back and independent penetration testing is answered **No**. SSDLC and SAST choices are **Yes**, with required evidence uploads still incomplete.

| Saved security answer | Explanation entered / verification boundary |
| --- | --- |
| TLS: **No** | HTTP is used only for the native loopback callback; the SDK manages encrypted UDP media transport. The Cloudflare minimum-TLS setting has not been independently verified. HTTPS API health does not prove every transport is TLS. |
| Webhooks: **No / N/A** | The app has no webhook subscriptions or deauthorization callback to validate. |
| Retention: **Yes** | Keychain credentials, local caches and explicit user exports are described separately from transient server-side OAuth/signing processing. Local retention is not hidden behind the service's lack of a user database. |

The Publish page identifies **App Icon, App Gallery and Application Overview** as missing. Attach the prepared icon, architecture and security evidence, and complete the pending native gallery capture. Saved answers and local files do not make the app ready for submission.

| Topic | Available evidence | Gap / truthful answer boundary |
| --- | --- | --- |
| Tests and change review | Current 0.1.1 [main CI](https://github.com/grinich/yap/actions/runs/34169878107) and [actual-SDK release tests](https://github.com/grinich/yap/actions/runs/34169899197) passed. Coverage includes callback/cancellation, Keychain, recording loader/parser, playback/account lifecycle and update-tampering tests; service fixtures cover PKCE/client pinning, grant/token binding, upstream refusal, limits and sanitized errors. | Attach the completed run reports and review record, then add the current Swift CodeQL result when complete. Passing tests do not establish a formal organization-wide secure SDLC. |
| Native artifact integrity | Current 0.1.1 [release run 34169899197](https://github.com/grinich/yap/actions/runs/34169899197) passed signing, notarization and Gatekeeper checks. Independent downloaded hashes, strict app/DMG signatures, stapled notarization, Gatekeeper and DMG integrity passed. App submission `0e889e8b-488f-43ff-97e8-5d8800edbb18`; DMG `5f0e64a5-b687-4ca4-b50a-4daab74215c2`. CryptoKit verified the downloaded ZIP's Ed25519 signature with its bundled public key. | Five artifacts remain in an unpublished draft; [current hashes and evidence](Releases.md#current-candidate-evidence) identify their exact bytes. This did not independently validate the entire feed signature, launch/install the app or complete an update. Fresh-Mac onboarding and N → N+1 update acceptance remain pending. |
| Authentication / data protection | PKCE/state/loopback validation, Keychain implementation, HTTPS media validation, bounded parsers, redirect credential stripping, stale-account guards; token-bound signer grants and live ZAK validation. Production browser consent/callback, token exchange with all three scopes, a bounded recordings-list request and SDK-signature response passed. | Live credentials stayed in test-process memory. Native GUI/Keychain integration, SDK initialization, refresh/revocation and new-user installation acceptance remain pending. User exports and every local cache are not claimed to be app-encrypted. |
| SAST / DAST / dependency scanning | [Merged-main CodeQL run 34166962123](https://github.com/grinich/yap/actions/runs/34166962123) passed on `ac394cd9a0834da2d3b58faed5e52efee980d246`. CodeQL **2.26.4** completed Swift analysis with **28 rules / 0 results** and JavaScript/TypeScript with **103 rules / 0 results**, without reported analysis errors or warnings. The repository query returned **zero open code-scanning alerts** at this checkpoint. CI functional tests and locked dependency controls exist. | SAST is selected Yes with these run and coverage distinctions; evidence upload remains required. DAST: no completed assessment. No dependency vulnerability-scan report is asserted. Swift scanning omits the proprietary SDK/conditional bridge path; CodeQL does not support Objective-C. |
| Independent penetration test | No report established. | Do not answer Yes or invent a vendor/date. |
| Vulnerability, patch and incident management | [Security policy](../SECURITY.md), adopted September 7, 2026: Michael Grinich is responsible, `mgrinich@gmail.com` is the private reporting channel, with review, patch, release and incident steps. | This is a documented lightweight process, not a certified program or proof of operational history. No recurring independent penetration test, SLA, SOC 2, ISO 27001 or HIPAA attestation is asserted. |
| Third parties / retention | Zoom SDK, Google Calendar, GitHub release/support hosting, Sparkle and Cloudflare Worker implementation/configuration. The production host and public review URLs are reachable. | Keep the recorded service deployment and scoped live checks with the release; complete the remaining native acceptance before asserting release readiness. |

Zoom's API terms require prompt incident notification, including a 24-hour outer limit after awareness. Assign a monitored security contact and an achievable incident procedure before claiming this operational control. [API terms, section 3](https://www.zoom.com/en/trust/legal/zoom-api-license-and-tou/)

## Portal handoff checklist

- Yap name, production contact, non-EU availability, technical stack, retention and GitHub documentation links are saved and confirmed. Publish identifies App Icon, App Gallery and Application Overview as missing. Complete those uploads, including the architecture/security evidence and the native gallery capture after the Mac is unlocked. Public Home/Support/Guide/Privacy/Terms URLs are reachable; use the portal’s verification process for GitHub links rather than claiming ownership of github.com.
- Preserve the recorded Worker deployment and scoped live authentication results. Complete native GUI/Keychain integration, SDK initialization, refresh/revocation and fresh-Mac acceptance; verify the packaged release uses the configured origin without requiring or containing the SDK secret.
- Read back the entered production public-client ID, native PKCE redirect allow list and three user-level scopes. Do not add unused event subscriptions or fabricate deauthorization callbacks.
- Attach an accessible immutable release and a realistic test plan/account. Complete all mandatory TDD answers honestly; explain gaps rather than checking Yes to pass validation.
- Submit the external app review. Record submission time, release version and status privately. If manual activation is chosen, approval alone does not activate external use. Respond to review notes through the portal; do not publish confidential review results. [Submission](https://developers.zoom.us/docs/distribute/app-submission/submit-apps-review/), [review notes](https://developers.zoom.us/docs/distribute/app-submission/check-review-status/)

The [Terms](https://github.com/grinich/yap/blob/main/TERMS.md) identify Michael Grinich, the free preview, authorized account/meeting use, recording consent, third-party rules, support and removal. They are effective September 7, 2026 and published in the GitHub repository at the operator’s request. No custom domain or service-hosted legal page is used. No provider contract has been accepted through this document.
