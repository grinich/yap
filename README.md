# Zooom

**Releases and updates:** the GitHub workflow and Sparkle updater are implemented; publishing still needs an approved download destination and signing credentials. [Release setup and acceptance checks](Documentation/Releases.md).

Private development repository: `grinich/zooom`. The Swift package lives at the repository root; the internal package and executable names remain `Whoosh`. Local verification links below refer to sibling `../outputs` and `../work` folders, which are intentionally not committed. The proprietary Zoom SDK and personal signing/account configuration are also excluded; see [Zoom setup](Documentation/Zoom-Setup.md) and [personal signing](Documentation/Personal-Signing.md).

**Bundle identity — September 7, 2026:** the macOS bundle identifier is `com.grinich.zooom`. The update migrates supported preferences from `com.grinich.woosh` and retains the existing secure credential namespaces. For the one-time installed app migration, use `Scripts/install-personal-app.sh --migrate-from-woosh`; future updates use the normal installer command. [Identity migration and verification](Documentation/Bundle-Identity.md).

**Recording library:** the Recordings button beside Close opens a sliding library of Zoom cloud recordings, with search, older months, native video playback, video-segment selection, and downloads. Double-click a recording for its own player window. The Chat control opens saved meeting messages on the right, following and highlighting them during playback. The Zoom app needs the `cloud_recording:read:list_user_recordings` scope and a fresh sign-in. [Setup, playback behavior, and verification limits](Documentation/Recordings.md).

**Latest minimal agenda checkpoint — September 6, 2026:** the date and main Upcoming heading are removed, with cards directly below the pinned top-right Start/Join controls. Native installed Mac screenshots verify normal and 320 × 272 compact layouts; the original window size was restored. Both app copies passed deep, strict signature checks, and all 339 functional tests in 46 suites pass. [Verification](../outputs/Zooom-Minimal-Agenda-Verification.md).

**Previous compact-window and reliability checkpoint — September 6, 2026:** the signed update is installed at `~/Applications/Zooom.app`; **339 functional tests in 46 suites pass** (build 7.40 seconds, tests 1.441 seconds). In a real muted, camera-off solo meeting at 320 × 272, clicking outside People search dismissed editing while preserving `MICH` and its visible result; clicking Chat while search was focused worked normally. [Installed evidence](../outputs/Zooom-Search-Focus-Installed.jpg). No new authorization prompts appeared, and the app returned to Agenda with no call. The audit also fixes stale-session Chat sends, cancelled media controls, key-sheet PiP handling, floating Chat display placement, unsupported calendar invitations and overlapping/stale calendar joins. Attached-sheet PiP behavior is verified only by the inert native fixture. Physical monitor unplugging, continuous press-drag menu selection and 100 live streams remain unverified. [Evidence and limits](../outputs/Zooom-Compact-Window-Verification.md).

**Previous compact-window checkpoint — September 6, 2026:** the final signed update is installed at `~/Applications/Zooom.app`. The content minimum is **320 × 240**, producing a **320 × 272 native window**; agenda, controls and Chat adapt, and compact notices scroll without hiding the composer. People’s left-aligned heading stays visible when controls fade; search ignores surrounding whitespace, case and diacritics and resets to the first result. Final real solo Zoom UI verified the visible filtered `MICH` row and persistent heading at normal and [compact size](../outputs/Zooom-Compact-People-Installed.jpg), microphone muted/camera off, without new authorization prompts. Earlier installed checks verified compact provider notices, avatars and selected Show all. **330 functional tests in 45 suites pass** (build 5.31 seconds, tests 1.466 seconds). Show all follows the entire roster; tests above 1,000 synthetic participants do not prove equivalent live-video capacity. Continuous native press-drag menu selection remains unverified. [Evidence and limits](../outputs/Zooom-Compact-Window-Verification.md).

**Previous immersive-meeting checkpoint — September 6, 2026:** the final signed update is installed at `~/Applications/Zooom.app`. Single/focused video fills the canvas, controls fade with pointer presence, and the grid defaults to **100 per page** with pagination beyond that. Chat/People have no separate X; their full-height glass reaches the top/right/bottom edges, with the heading immediately left of the fixed header buttons. The installed real solo meeting verified this [Chat header](../outputs/Zooom-Chat-Header-Installed.jpg), a single “People per page” heading, and clean End for Everyone back to the populated agenda, with microphone muted/camera off and no new authorization prompt. **323 functional tests in 44 suites pass** (build 4.66 seconds, tests 1.443 seconds). The preceding same-renderer build verified real edge-to-edge camera video; the final header-only build did not repeat that camera check. The [100-person gallery](../outputs/Zooom-100-Participant-Preview.jpg) is synthetic, not proof of 100 live feeds. [Evidence and limits](../outputs/Zooom-Immersive-Meeting-Verification.md).

**Previous Zooom rebrand checkpoint — September 6, 2026:** **Zooom** (three o’s) is installed at `~/Applications/Zooom.app`, with the new pearl-white glass Z icon verified in the native About window. **313 functional tests in 41 suites pass** (build 2.58 seconds, tests 1.458 seconds). The internal Whoosh executable/modules, `com.grinich.woosh` bundle ID, Developer ID, credentials, preferences, and URL scheme remain unchanged. Saved settings and both connections were retained without authorization prompts; a real muted, camera-off solo meeting started and ended successfully. Raw video, audio, and sharing were not retested. [Rebrand evidence and limits](../outputs/Zooom-Rebrand-Verification.md), [installed icon/name](../outputs/Zooom-Installed-About.jpg), [hosting](../outputs/Zooom-Hosting-Verified.jpg). Earlier Whoosh-named checkpoints below are historical.

**Previous minimal-header checkpoint — September 6, 2026:** the final signed app is installed and **313 functional tests in 41 suites pass** (build 3.82 seconds, tests 1.471 seconds). The installed real Upcoming agenda shows right-aligned Start/Join and one gray close button; Join opened the editable sheet and Cancel returned to the agenda. Saved calendar access resumed without Keychain/TCC prompts. Native inert QA separately verified minimum-width Light/Dark layout, same-instance window hide/restore, close-button/⌘W meeting confirmation, and one-shot idle close. No new Zoom-media or physical status-bar-click acceptance is claimed. [Evidence and limits](../outputs/Whoosh-Minimal-Header-Verification.md).

**Previous titlebar checkpoint — September 6, 2026:** the final release build and canonical installer completed; **307 functional tests in 40 suites pass**. A persistent hidden-titlebar scene and reapplied window appearance prevent the white joining header and generic app title. Native non-preview inert QA visually verified Agenda → empty-toolbar Joining → empty-toolbar Meeting → two-item Agenda in Light, plus Dark Joining; the white strip and top app title were absent. This uses no accounts or Zoom and does not verify physical desktop-color transfer or a new live call. [Evidence and limits](../outputs/Whoosh-Titlebar-Verification.md).

**Previous main-window material checkpoint — September 6, 2026:** the release build and canonical installer completed; **307 functional tests in 40 suites pass**. Agenda and Meeting share one active native behind-window backdrop, without the Agenda's former 88%-opaque veil. Native synthetic QA verified Light Agenda, Dark six-participant Meeting with Chat, and the compact solid Reduce Transparency fallback with all five controls visible. Diagnostics confirmed one full-window effect in normal mode and none in the fallback. Actual physical desktop color transfer remains visually unverified because isolated CUA captures flatten the window; this adds no live Zoom media or HIG-certification claim. [Evidence and limits](../outputs/Whoosh-Main-Window-Glass-Verification.md).

**Previous notification-permission checkpoint — September 6, 2026:** the signed update is installed and **307 functional tests in 40 suites pass**. Reminder setup explains permission in a centered app sheet, avoids repeated denied requests, rechecks an explicit Settings visit, and cancels stale work without a generic global authorization alert. Native fixture QA verified Light/Dark intro and denied states, simulated Settings approval, and Escape cancellation. In the installed already-authorized app, Off → On restored reminders without a sheet, error, or repeat OS request; On was retained. The actual Allow prompt, real denied-to-allowed Settings change, and notification delivery remain unverified. macOS controls its own dialog placement. [Evidence and limits](../outputs/Whoosh-Notification-Permission-Verification.md).

**Previous video-return checkpoint — September 6, 2026:** the final signed update is installed; **296 functional tests in 39 suites pass**, plus a separate native bridge fixture. PiP keeps a stable logical video surface. The fixed 1.5-second bound-renderer resume wait is removed; a real muted solo camera-off/on check logged successful show and subscription at the same millisecond timestamp, without retry/failure. A final Settings → ⌘0 capture at 587 ms showed active macOS camera-background output. That command-to-screenshot time includes tool overhead and is not first-frame latency; initial camera warmup was not instant. Camera was restored off. The preceding geometry build's three captures remain separately labeled. [Evidence and limits](../outputs/Whoosh-Video-Return-Verification.md).

**Previous solo PiP checkpoint — September 6, 2026:** the signed update is installed, and **292 functional tests in 39 suites pass**. Solo meetings now show the local participant in PiP when no remote participant is available. A real muted one-person meeting produced a mounted, ready 112 × 63 local camera renderer; ⌘0 preserved that renderer and restored visibly working camera video in the main window, then the camera was turned off. Physical floating-panel pixels were not directly visible through the screenshot tool. Remote arrival/departure switching is functional-test coverage only on this build. [Exact evidence and limits](../outputs/Whoosh-Solo-PiP-Verification.md).

**Previous window and controls checkpoint — September 6, 2026:** the signed update is installed. Meeting surround uses native behind-window blur with opaque video and a Reduce Transparency fallback; Record moved beside Share; Leave is more subtle in Dark; Return/Open uses a deferred, confirmed-key-window presenter. **290 functional tests in 39 suites pass.** Native recording-control/compact-layout checks and installed hosting/Settings-to-meeting restoration passed. Direct physical menu-bar clicking and desktop color transfer remain separate verification limits. [Details](../outputs/Whoosh-Window-and-Controls-Verification.md).

**Previous polish checkpoint — September 6, 2026:** the signed build is installed. **283 functional tests in 38 suites pass.** Chat preserves history-reading position; calendar headings/dates update across day boundaries; Join has predictable keyboard focus and whitespace handling; recording requests show immediate pending feedback; Share/Switch help matches the action. Native synthetic chat QA and installed keyboard/future-agenda checks passed. The new UI checks do not constitute a new live Zoom media run. [Acceptance audit and remaining work](../outputs/Whoosh-Next-Acceptance-Tests.md).


**Previous Liquid Glass checkpoint — September 6, 2026:** the signed release build passed in **9.97 seconds**, with **97 embedded Mach-O images**, **three App Intents**, and **three App Shortcuts** verified. The final build is installed at `~/Applications/Whoosh.app` and reopened successfully with saved accounts intact. [Release log](../work/liquid-glass-release-final.log). The SDK-disabled functional suite remains **271 tests in 36 suites**, with zero failures, skips, or compiler warnings; incremental build **2.92 seconds**, tests **1.481 seconds**.

Start and Join now occupy the native top toolbar. The agenda uses a semantic window material with an 88% readability tint, standard-material cards, and dates for distant meetings. Meeting controls share one native glass dock and follow system Light/Dark appearance; Chat uses a rounded semantic inspector, unified composer, and circular native glass Send. Floating Chat confines glass to its titlebar and keeps its content opaque.

Final installed agenda preview verified the native toolbar and corrected contrast ([sample-data screenshot](../outputs/Whoosh-Liquid-Glass-Agenda-Preview.jpg)); the app was then returned to the live agenda. Initial installed interface-preview checks showed the dock and Chat, including sample Return-send. Separate **manual synthetic UI QA** of the exact current `WhooshRootView`, linked with SDK-disabled objects, checked a 960-point Light meeting/chat layout and all four controls fitting at 640 points in Dark, with local Reduce Transparency, Increased Contrast, and Reduce Motion overrides. [Light fixture](../outputs/Whoosh-Liquid-Glass-Light.jpg), [accessible 640-point fixture](../outputs/Whoosh-Liquid-Glass-Accessible-640.jpg). These are fixture screenshots, not automated visual tests or live Zoom calls; no global OS settings changed, and no HIG certification is claimed. The actual status-bar label and right-click Open/focus path remain unverified.

**Prior installed checkpoint:** the signed `com.grinich.woosh` app is installed at `~/Applications/Whoosh.app`. All **271 tests in 36 suites** pass. The preceding cloud-recording release verified Cloud Record, Pause, Resume, and Stop in a real own-account meeting, with only synthetic window content and camera/microphone off. The native recording indicator was verified on the Mac. Processed cloud-file playback remains unverified. [Recording verification](../outputs/Whoosh-Cloud-Recording-Verification.md).

**September 6, 2026 — timed and named Join:** the signed release is installed. Calendar Join controls appear from five minutes before the scheduled start until the meeting ends. Distant events had no Join control in the actual agenda; a near-five-minute preview event displayed Join. [Agenda evidence](../outputs/Whoosh-Join-Timing-Verified.jpg), [release log](../work/named-join-release.log), [install log](../work/named-join-install.log). The named menu-bar action (`Join <title>`) and stale-event protection are covered by tests. Open now defers until menu tracking finishes and restores/focuses newly attached or minimized main windows; this AppKit path and the actual status-bar label have not been directly verified.


A personal native macOS meeting client in development. Targets macOS 26 and Apple silicon, using SwiftUI and AppKit.

**Status: active integration testing.** Direct Google Calendar, own-account Zoom hosting, Zoom-only agenda, native window previews, inline Join feedback, and the final window-sharing/PiP/Stop path have live evidence. The final received PiP stream measured **160 × 90 at 11–12 fps**, with outbound camera video **640 × 360 at 26 fps**. [Receiver evidence](../outputs/Whoosh-Companion-Share-Receiver.jpg), [companion details](Documentation/Sharing-Companions.md).

Earlier builds separately verified local/incoming gallery video, two-way chat, outgoing A → B source switching with matching local/remote labels, incoming animated sharing, and clean exits. Separate historical intervals measured outgoing 720p at 24–30 fps and incoming 720p at approximately 22–30 fps; these do not describe the final run or establish simultaneous two-way HD/1080p. Repeated gallery/focus/camera transitions, audio correctness, guest entry, reconnect, and live-feed capacity remain separate acceptance checks. [Video history](Documentation/Video-Runtime.md).

## Build and test

Requires Xcode 26.6 with Swift 6.3.3 or newer. SwiftPM resolves the pinned Sparkle 2.9.6 updater from `Package.resolved`. The proprietary Zoom SDK is downloaded separately and remains outside source control.

```sh
git clone git@github.com:grinich/zooom.git
cd zooom
WHOOSH_ZOOM_SDK_PATH=/nonexistent bash Scripts/test.sh --scratch-path /tmp/whoosh-pure-tests
bash Scripts/build-app.sh release
# Quit Zooom (or the previous Whoosh version) before installing.
bash Scripts/install-personal-app.sh
```

The installer copies the verified package to `~/Applications/Zooom.app`, outside the development workspace in Documents. It requires the configured persistent certificate fingerprint, refuses a running app or a different installed bundle/signing identity, and verifies a staged copy before replacement. It unregisters the development package and registers the canonical installed copy with Launch Services. It does not launch Zooom or change macOS permissions or trust. A same-identity installation at `~/Applications/Whoosh.app` is migrated to the new name; the installer refuses ambiguous duplicate installations. The one-time `--migrate-from-woosh` option accepts the previous `com.grinich.woosh` bundle only after verifying its existing Apple/team designated requirement. The older `--migrate-from-personal` option accepts only the approved old identity and new Developer ID identity. Normal updates require the standard Apple/team designated requirement. Keychain service names and the `whoosh` URL scheme remain compatible; macOS authorization is still required for existing protected resources under a new identity.

The build produces `../outputs/Zooom.app`, including native icon assets, extracted shortcuts, and the Zoom SDK runtime. It stages and signs the complete package before replacement. The historical PiP release compiled in **10.62 seconds**, verified **97 embedded Mach-O images**, extracted **three App Intents** and **three App Shortcuts**, and passed strict signing. The package was installed at `~/Applications/Whoosh.app` and launched successfully. [Package log](../work/pip-verified-release.log), [install log](../work/pip-verified-install.log). Notarization has not been performed.

`../work` holds temporary build caches. The app-icon source is `Resources/Whoosh.icon`, compiled by Apple's asset tool into Assets.car and ICNS; `Resources/WhooshIcon.png` remains the in-app logo. The new Z artwork uses the same internal resource names; its compiled system icon and installed native About appearance are verified. [Current icon/name evidence](../outputs/Zooom-Installed-About.jpg). A separate current physical Dock screenshot was not captured. The previous W icon was visually verified without the former pale outer plate or double inset. [Historical Dock evidence](../outputs/Whoosh-Dock-Verified.png).

The test command disables the proprietary SDK and enables synthetic local OAuth callback tests. These open temporary listeners on `127.0.0.1` and do not contact Google or Zoom. Run with local-listener permission. The current compact-window and reliability checkpoint passed **339 tests in 46 suites** on September 6, 2026, with **zero failures, skips, or compiler warnings**; incremental build **7.40 seconds**, tests **1.441 seconds**. Coverage now includes pointer state/input, aspect-fill geometry, dynamic Show all and interactive titlebar regions. The existing synthetic 144-person regression verifies the 100-person default and pagination; it does not establish simultaneous live-feed capacity. Coverage includes five-minute/end-time Join boundaries, named/stale menu actions, calendar/bootstrap/revocation, foreground refresh/coalescing, hosting privacy/retry/cancellation, credential safety, combined share-source ordering and picker interaction, Join-sheet feedback, confirmed sharing-source labels, renderer handoff, preference migration, menu-bar authority, and sharing-companion state/geometry/chat/Stop-return. The SDK is disabled in this run; the Objective-C readiness fixture and live video/capture checks are separate. [Current test summary](../outputs/Whoosh-Pure-Swift-Test-Summary.json), [build status](../outputs/Whoosh-Build-Status.md), [verification record](../outputs/Whoosh-Integration-Verification.md).

## Interface and permissions

Settings → Development → Explore interface preview uses sample events and avatar tiles. It exercises layouts and control state without enabling a camera or microphone; preview chat/sharing never reaches another device. A grid of 100 sample tiles is not proof of 100 simultaneous live video feeds.

A connected meeting can move into a floating participant strip when the main window is backgrounded or minimized. While sharing, separate Chat and menu-bar Stop remain available. Native synthetic QA verified dragging, pointer fade, and Return-send; the final real meeting verified minimized PiP and Stop's main-window restoration. [Sharing companions](Documentation/Sharing-Companions.md) records the native references and capture limits. Full-display exclusion is not guaranteed.

Zooom-owned credentials use a shared, cached Keychain vault, with legacy migration through normal macOS approvals. [Credential storage](Documentation/Credential-Storage.md). After the historical self-signed recurrence and initial Developer ID authorization, two successive changed Developer ID builds at `~/Applications/Whoosh.app` loaded both accounts and hosted without any new Keychain or TCC prompt. The read-only vault check now shows `teamid:VSVHNQP588`. The app does not broaden credential ACLs, change trust, or bypass authorization. Earlier screen-recording recovery used normal user-authorized System Settings; Accessibility remained off and untouched. These observed successes do not promise that all future OS prompts are eliminated. [Personal signing](Documentation/Personal-Signing.md).

## Account setup and remaining verification

- [Google Calendar](Documentation/GoogleCalendar.md): personal setup, real read-only consent, selection/refresh, Zoom-only agenda, and cache behavior.
- [Zoom integration](Documentation/Zoom-Setup.md): OAuth/ZAK, own-account hosting, SDK contracts, delivered sharing, and remaining acceptance checks.
- [Video evidence](Documentation/Video-Runtime.md): measured media, received/outgoing sharing, latest renderer visibility, and remaining camera checks.
- [Native interface audit](Documentation/macOS-HIG-Audit.md): implemented corrections, observed results, and accessibility/appearance follow-through.

Developer credentials, OAuth tokens, and vendor frameworks stay out of source control. The personal runtime SDK-signing path must be replaced before distributing shared developer credentials. Guest entry, audio, reconnect/sleep-wake, simultaneous two-way HD/1080p, feed limits, full-display sharing/panel exclusion, and a complete accessibility walkthrough remain unverified. CUA restores another physical foreground app after some actions, so this run proves the meeting returned onscreen, not persistent foreground activation. Light/dark and selected/neutral controls have separate actual visual evidence. The release ZIP passed CRC checks and its executable/Info.plist match the canonical installed app. The renamed release and source archives use Zooom filenames; the source archive retains the internal `Whoosh/` project folder and excludes credentials, vendor binaries, runtime logs, and screenshots. [Release manifest](../outputs/Zooom-Release-Archive-Manifest.json), [source manifest](../outputs/Zooom-Source-Archive-Manifest.json).
